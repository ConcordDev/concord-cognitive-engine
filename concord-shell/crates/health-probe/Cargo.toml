[package]
name = "concord-shell-health-probe"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Dependency-free TCP/HTTP liveness probing for the Concord desktop shell. Uses only std::net -- no tauri, no async runtime, no system GUI library -- so it builds and tests standalone in any environment, verified against real local TcpListeners in its own test suite."

[dependencies]
