{
  "macro_id": "society.wb-region-rankings",
  "domain": "society",
  "name": "wb-region-rankings",
  "inputs": {
    "indicator": { "type": "string", "maxLength": 80 }
  },
  "invariants": [
    "typeof output === 'object' && output !== null",
    "typeof output.ok === 'boolean'"
  ],
  "fuzz_cases": [
    { "input": {}, "expect": { "ok": false, "error": "indicator required" } },
    { "input": { "indicator": "<script>alert(1)</script>" }, "expect": { "ok": false, "error": "indicator must be a World Bank code or alias" } }
  ],
  "_note": "Ranks every World Bank region + income-group aggregate by one indicator. Fuzz_cases land on PRE-NETWORK validation: missing indicator, then the format guard blocking an XSS indicator before the WB URL is built. NEVER no_db (no DB)."
}
