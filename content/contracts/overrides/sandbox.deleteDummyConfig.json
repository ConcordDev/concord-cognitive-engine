{
  "macro_id": "sandbox.deleteDummyConfig",
  "domain": "sandbox",
  "name": "deleteDummyConfig",
  "inputs": { "configId": { "type": "string", "maxLength": 80 } },
  "invariants": [
    "typeof output === 'object' && output !== null",
    "typeof output.ok === 'boolean'",
    "output.ok === false || (output.result && typeof output.result.count === 'number' && output.result.count >= 0)"
  ],
  "fuzz_cases": [
    { "input": {}, "expect": { "ok": false, "error": "dummy_config_not_found" } },
    { "input": { "configId": "nope" }, "expect": { "ok": false, "error": "dummy_config_not_found" } }
  ],
  "_note": "Removes one of the caller's saved dummy configs by id from per-user STATE. A missing/unknown id fails-closed with 'dummy_config_not_found' (NEVER no_db). The assassin's internal owner actor has none → dummy_config_not_found."
}
