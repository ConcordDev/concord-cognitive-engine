{
  "macro_id": "personas.versions",
  "domain": "personas",
  "name": "versions",
  "inputs": {
    "personaId": {
      "type": "string",
      "maxLength": 120
    }
  },
  "invariants": [
    "typeof output === 'object' && output !== null",
    "typeof output.ok === 'boolean'",
    "output.ok === false || Array.isArray(output.versions)"
  ],
  "fuzz_cases": [
    {
      "input": {},
      "expect": {
        "ok": false
      }
    }
  ],
  "_note": "personas.versions now delegates to the real server/domains/personas.js LENS_ACTION (wired 9370fc2d6 \u2014 the Z4 single-version stub is retired). With no personaId it HONESTLY returns { ok:false, error:'persona_not_found' }; the real macro is author/visibility-gated and returns result.versions with a per-revision changelog. Invariant still holds: output.ok === false || Array.isArray(output.versions)."
}
