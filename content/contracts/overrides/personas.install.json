{
  "macro_id": "personas.install",
  "domain": "personas",
  "name": "install",
  "inputs": {
    "personaId": {
      "type": "string",
      "maxLength": 120
    }
  },
  "invariants": [
    "typeof output === 'object' && output !== null",
    "typeof output.ok === 'boolean'",
    "output.ok === true || typeof output.reason === 'string' || typeof output.error === 'string'"
  ],
  "fuzz_cases": [
    {
      "input": {},
      "expect": {
        "ok": false
      }
    }
  ],
  "_note": "personas.install now delegates to the real server/domains/personas.js LENS_ACTION (wired 9370fc2d6 \u2014 the Z4 roadmap stub is retired). With no authenticated actor it HONESTLY returns { ok:false, error:'no_actor' }; a real install (increments installCount on a published persona, drives the royalty cascade) needs a signed-in caller + a valid personaId."
}
