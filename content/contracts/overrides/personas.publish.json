{
  "macro_id": "personas.publish",
  "domain": "personas",
  "name": "publish",
  "inputs": {
    "personaId": {
      "type": "string",
      "maxLength": 120
    },
    "published": {
      "type": "boolean"
    }
  },
  "invariants": [
    "typeof output === 'object' && output !== null",
    "typeof output.ok === 'boolean'",
    "output.ok === true || typeof output.reason === 'string' || typeof output.error === 'string'"
  ],
  "fuzz_cases": [
    {
      "input": {},
      "expect": {
        "ok": false
      }
    }
  ],
  "_note": "personas.publish now delegates to the real server/domains/personas.js LENS_ACTION (wired 9370fc2d6 \u2014 the Z4 roadmap stub is retired). With no authenticated actor it HONESTLY returns { ok:false, error:'no_actor' }; the real publish toggles marketplace visibility and is author-gated."
}
