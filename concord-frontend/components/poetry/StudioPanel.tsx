'use client';

/**
 * StudioPanel — form templates + live checking, audio readings, chapbook export.
 */

import { PoetryStudio } from '@/components/poetry/PoetryStudio';

export function StudioPanel() {
  return <PoetryStudio />;
}
