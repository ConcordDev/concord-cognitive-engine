# Wave 4 Inventory — Deferred Capabilities Across All 257 Lens Capability-Map Docs

Generated 2026-07-11 as the first step of Wave 4 ("closing the hard 20%" — CLAUDE.md's sixth
hard invariant). This is a **docs-only extraction pass**: every `docs/lens-specs/*-capability-map.md`
file was read in full and every item the doc itself frames as genuinely missing, deferred, or
scoped-out was pulled into one ledger with a DATA-SOURCING / ENGINEERING / CURATION triage, per
the invariant's rule:

- **DATA-SOURCING** — needs a real free/open external data feed/API.
- **ENGINEERING** — a missing feature/endpoint/UI with no external data dependency — just needs
  building.
- **CURATION** — real open/authoritative reference material exists but needs authoring or
  ingesting into Concord.

No lens source code was re-read for this pass — Wave 3's capability-map docs already did that
research; this is purely mining what was written down. Positive "already real" findings are
excluded; only items the source doc itself calls out as not-done are listed. **259 items** were
extracted across 194 lenses; **63 lenses** had zero deferred items per their own doc (fully
closed). Table is sorted alphabetically by lens (matches capability-map filename order).

This inventory is the input to gap-closure work, not the closure itself — each row is a
candidate unit of future work, not yet actioned. `docs/lens-specs/README.md` should link here
once this lands.

## Concordia (3D world) findings — added 2026-07-11, second-half of this Wave

The flat-lens sweep above covers 260 lens directories but explicitly excluded Concordia's own
world-simulator, per `docs/lens-specs/world-capability-map.md`'s own admission that it's a
"summary-level backfill, not an exhaustive audit." A dedicated 8-subsystem pass closed that gap:
`docs/concordia-specs/{lore-worldbuilding,quests-dialogue,factions-politics,combat-feel-residuals,
crafting-economy-housing,minigames,runmodes-endgame-social,runtime-health}-capability-map.md`,
each benchmarked against a named genre leader (CK3, Witcher 3, WoW, Hades, etc.) with the same
re-verify-against-live-code discipline. Three small, safe residuals were fixed inline during that
pass (kingdom-decrees ruler-authorization gap, hidden-object/farming/restaurant minigame juice —
see `docs/FRONTEND_REBUILD_PROGRAM.md`'s ledger). Below is the prioritized remainder, worst-first;
full detail and file:line evidence lives in the source docs.

| Priority | Item | Triage | Source doc |
|---|---|---|---|
| 🔴 Live-broken | ~~Time-loop mode: 3 of 5 HTTP routes 404 (missing `/` before path params, verified live) — HUD never renders, loops can't be ended from the UI~~ **CLOSED (2026-07-12)** — the "3 routes 404" half never reproduced (`git log --all -S` on the broken route string returns zero commits in this repo's history; all 5 routes have always had correct leading slashes, HUD renders fine). The real half — no "End loop" UI call site — is now fixed: `TimeLoopHUD.tsx` POSTs the real `endLoop` route with `reason:'manual_exit'`, clearing the HUD only on a confirmed `ok:true`. | ENGINEERING | runmodes-endgame-social |
| 🔴 Live-broken | ~~`getRelationScore()` hardcoded to `return 0` — silently breaks `PROPOSE_ALLIANCE` and relation-gated `DECLARE_WAR` targeting fleet-wide~~ **CLOSED (2026-07-12, `5be60991`)** — now reads the real `faction_relations` score via the live `db` handle the heartbeat threads into `pickMove`; neutral `0` preserved only for tests that deliberately call `pickMove` without a `db`. | ENGINEERING | factions-politics |
| 🔴 Live-broken | ~~`moral_branch`/`reputation_change` authored across 11 quest files, read by zero lines of code anywhere — the "meaningful choice" mechanic never fires for a player~~ **PARTIALLY CLOSED (`0c0c57e1`), frontend half investigated 2026-07-12 and confirmed genuinely unreachable, NOT force-fixed** — the backend half stands (`quest.resolve_moral_branch` applies a chosen option's `reputation_change` to the real reputation substrate). A Wave 4 unit investigated the frontend half and independently re-confirmed `docs/QUESTS_ENGINE_INVESTIGATION.md`'s finding with a fresh empirical run (full real migration set, `seedContent()`, live DB inspection): the 14 moral_branch-bearing authored quests (across the 11 files) have **zero rows** in `world_quests` — the only table any live dialogue-offer/quest-tracker surface reads — so no in-game quest-step reveal ever reaches them. Of their giver NPCs, only 2 of 14 (`lady_seraphine_voss`, `broker_silver_vey`) have any authored dialogue tree at all, and both are generic `:idle` ambient chatter with no quest-specific node — attaching a climactic reputation-altering choice to an idle greeting with zero preceding narrative setup would misrepresent story causality to the player, a narrative-honesty problem distinct from data fabrication but the same spirit. The one already-live frontend surface reading System A's quest registry (`/lenses/maker`'s "Quests" tab, `quest.list`/`.active`/`.metrics`) is an explicitly-labeled world-builder/content-authoring tool (Twine/Retool-parallel), not a player-facing narrative surface — wiring player choice into it would misrepresent a builder tool as a story beat. No frontend surface was built; see `docs/concordia-specs/quests-dialogue-capability-map.md` §3 for the full trace. | ENGINEERING | quests-dialogue |
| 🔴 Live-broken | ~~AvatarSystem3D + ConcordiaScene effect-thrash — both heaviest 3D components fully teardown/rebuild on nearly every render during ordinary movement/combat; root cause of any world-lens jank~~ **CLOSED (`3563714b`)** | ENGINEERING | runtime-health |
| 🔴 Live-broken | ~~`window.__concordiaPlayerPos` read by 9+ production files, written by none — Extract button can never enable via ExtractionRunHUD, scheme-overhear earshot gate bypasses itself~~ **CLOSED (`f7e86bec`)** | ENGINEERING | runtime-health |
| 🔴 Live-broken | ~~SoundscapeEngine ambient noise layer leaks a new infinite audio loop on every district/time/interior change — directly audible (hiss compounds over a session)~~ **CLOSED (`f7e86bec`)** | ENGINEERING | runtime-health |
| 🟠 Real, unreachable-but-built | ~~Guild substrate — complete DB-backed bank/XP/hall system, 100% unreachable (zero callers outside its own tests)~~ **CLOSED (`e459dec3`)** | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~World bosses never spawn in production (scheduler has zero callers outside tests)~~ **CLOSED (`49fe646c`)** — also fixed a deeper bug the original audit missed: the heartbeat never forwarded `worldId` to the handler at all. | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~`spawnFactionWar` (real combat pipeline) only reachable via admin/test endpoint, never triggered by autonomous war/raid moves~~ **CLOSED (2026-07-12, `4bd4327a`)** — `faction-strategy-cycle.js` now calls it after a `DECLARE_WAR`/`RAID` move persists, idempotent via `findActiveWarBetween()`, kill-switch `CONCORD_FACTION_WAR_SPAWN`. | ENGINEERING | factions-politics |
| 🟠 Real, unreachable-but-built | ~~Roguelite shop catalog and effect-engine catalog share zero item IDs (purchases do nothing); price is client-supplied — security-adjacent~~ **CLOSED (`dd7b1b03`)** | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~Party-combat "ability" action has no server-side ability catalog — damage is a caller-supplied number — security-adjacent~~ **CLOSED (`f9a2c6c1`)** — real `PARTY_ABILITY_CATALOG`, one signature ability per combat-polish profile. | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~Dungeon instances — real phase-gated boss engine (HP%-thresholded phases, per-member damage, loot-by-share, lockouts) with zero frontend caller; `recordHit`'s damage report had no upper bound (security-adjacent)~~ **CLOSED (2026-07-12, Wave 4 gap-closure unit)** — new `DungeonHUD.tsx` (encounter browser, live boss hp%/phase/party-damage panel, real loot-share result banner) + two new discovery macros (`dungeon.active`, `dungeon.lockouts`) + `recordHit` now rejects any report above the shared `resolvedDamageCap()` ceiling instead of trusting it. | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~Horde's wave-upgrade picker writes to a table nothing reads — picking a "boon" has zero mechanical effect on combat~~ **CLOSED (2026-07-12, Wave 4 run-mode gap-closure unit)** — now delegates to the shared draft engine (below) and genuinely applies `damageMult`/`critChance` to both live combat paths (the DB-backed skill-cast REST route AND the socket-driven basic-attack path traced as the actual live "Fight `<npc>`" affordance). | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~`run-draft.js` — a complete structured-boon draft engine with synergy combos and fair deterministic rolls — has zero callers anywhere~~ **CLOSED (2026-07-12, Wave 4 run-mode gap-closure unit)** — now powers horde's wave-clear pick (above) and a new roguelite in-run "Descend" draft moment (`advanceRun`/`pickDraftBoon`, banking `1 + extraDraftPicks` picks per advance). | ENGINEERING | runmodes-endgame-social |
| 🟠 Real, unreachable-but-built | ~~Roguelite's `runMetaModifiers()` computes a correct meta-unlock bundle from owned purchases that `startRun` returns and nothing reads back — `startingHpBonus`/`damageMult`/`metaCurrencyMult`/`revives` were all inert.~~ **NOT in the original 2026-07-11 audit — found independently while closing the two rows above, and CLOSED in the same Wave 4 unit (2026-07-12).** All five now apply for real: HP bonus added/removed symmetrically around `player_resource_bars` (migration 359), `damageMult` folds additively into the shared run-modifier bundle, `metaCurrencyMult` multiplies the `endRun` cash-out, and `revives` is consumed by a new `maybeReviveRoguelitePlayer` wired into both the HTTP `combat/npc-attack` route and `npc-simulator.js`'s autonomous NPC-attack heartbeat (the two independent live sources of lethal player damage). See `docs/concordia-specs/runmodes-endgame-social-capability-map.md` §2.3 "Gap C" for full detail. | ENGINEERING | runmodes-endgame-social |
| 🟠 Real bug | ~~Ragdoll physics bodies never freed (`despawnRagdoll` called but doesn't exist; real method is `removeRagdoll`) — every kill leaks 7 bodies, compounded by duplicate listeners per world switch~~ **CLOSED (`fa16331e`, `1492b380`)** | ENGINEERING | runtime-health |
| 🟠 Real bug | ~~Building collapse has full visual/audio wiring, zero physics-side consumer — player walks into an invisible wall where a collapsed building stood~~ **CLOSED (`49783550`)** | ENGINEERING | runtime-health |
| 🟠 Real bug | ~~Procedural building materials/textures leak unbounded at module scope — the path most real buildings render through~~ **CLOSED (`a0122dd1`)** | ENGINEERING | runtime-health |
| 🟠 Real bug | ~~`AudioContext.close()` never called on unmount — repeated world-lens navigation stacks contexts, hits browser caps~~ **CLOSED (`f7e86bec`)** | ENGINEERING | runtime-health |
| 🟠 Real bug | ~~6 polling HUDs cache `activeWorldId` once at mount, go stale on same-tab world travel (fix already exists in-repo as `useActiveWorldId()` — just needs migrating)~~ **CLOSED (`6e362f7e`)** | ENGINEERING | runtime-health |
| 🟡 Content depth | Procedural NPC personality: only 10 traits/~31 secrets recycled across 74 NPCs; every one shares an identical closing backstory line | CURATION | lore-worldbuilding |
| 🟡 Content depth | 29% (26/90) of cross-world "resonance" links point at NPCs that were never written | CURATION | lore-worldbuilding |
| 🟡 Content depth | Only 15 of 136+ authored NPCs have bespoke dialogue trees (main arc's own named NPCs aren't among them) | CURATION | quests-dialogue |
| 🟡 Content depth | `content/world/sere/` — the best-written content in the game — is undocumented, not in CLAUDE.md's "9 sub-worlds" claim | ENGINEERING (docs) | lore-worldbuilding |
| 🟡 Content depth | `tunya/factions.json` names a fictional faction "Cree" — collides with a real living Indigenous nation | CURATION | lore-worldbuilding |
| 🟡 Content depth | Mahjong: no chi/pon/kan/riichi (real engine, genuinely deepest minigame, but incomplete ruleset) | ENGINEERING | minigames |
| 🟡 Content depth | Trivia bank is exactly 30 questions across 45 mostly-singleton tags — too small for repeat play | CURATION | minigames |
| 🟡 Content depth | Romance/marriage (deepest mechanical system found — courtship→marriage→pregnancy→heir inheritance) has only 3 generic heart-event scenes for the entire cast | CURATION | runmodes-endgame-social |
| 🟢 Small | ~~4 authored multi-step craft chains never loaded by any seeder (dead content)~~ **CLOSED (`080fe557`)** — `content-seeder.js` now walks every `content/world/<world>/recipes/chains.json`. | ENGINEERING | crafting-economy-housing |
| 🟢 Small | ~~Resource-catalog/market-catalog ID mismatch — only 3 of ~30 IDs overlap, most traded materials fall back to generic properties~~ **CLOSED (`dec6d05b`)** — 4 real aliases reconciled. | ENGINEERING | crafting-economy-housing |
| 🟢 Small | ~~Hacking's free unlimited hints can make the authored terminal tree trivially skippable~~ **CLOSED (`38bdb96f`)** — hint-spam gated (migration 358). | ENGINEERING | minigames |
| 🟢 Small | ~~Karaoke scores pitch-consistency not melody accuracy (songs already carry an unused `key` field — cheap fix)~~ **CLOSED (`38bdb96f`)** — same commit as the hacking-hint fix. | ENGINEERING | minigames |
| 🟢 Small | Mahjong legacy checkbox resolver route still reachable (non-exploitable, but an unverified-success path) | ENGINEERING/CURATION | combat-feel-residuals |
| 🟢 Small | ~~~9 more lower-severity runtime findings (vehicle-renderer no per-despawn dispose, WaterRenderer texture leak, duplicate socket connection, crafting-minigame cancel-doesn't-cancel, etc.)~~ **PARTIALLY CLOSED** — vehicle-renderer dispose (`3890d2b9`), WaterRenderer texture leak (`de0980ec`), duplicate socket connection (`d7896339`), and crafting-minigame cancel-doesn't-cancel (`ed445e3e`) are all fixed. The doc's own "Lower severity" bullet list (TreeLayer instance-pool dispose, uprising-crowd pole material, stem-oscillator/music-interval unmount coverage, etc.) remains open — no matching fix commits found. | ENGINEERING | runtime-health |

Not repeated here: the ~15 LOW-severity/cosmetic runtime findings and the several items each
subsystem doc confirmed already fixed or false-positive — see the source docs for full detail.

## Full ledger — flat lenses

| Lens | Item | Reference Benchmark | Triage | Doc Disposition (source quote) | Source Doc |
|---|---|---|---|---|---|
| accounting | ~~No real mobile affordance for `BooksShell`'s sidebar navigation~~ **CLOSED (2026-07-12, `3e49176f`)** — the exact follow-up the capability-map named after the generic-scaffold cleanup deleted `MobileTabBar`: desktop unchanged (the w-44 sidebar is now `hidden md:flex`), below `md` a compact header row shows the active destination plus a hamburger opening the same grouped nav as a slide-over drawer; selecting a destination navigates and closes the drawer, close button/backdrop dismiss without navigating. Tests: `BooksShellMobileNav.test.tsx` 4/4 + both pre-existing accounting test files re-run clean (14/14 combined). | QuickBooks Online / Xero-class | ENGINEERING | closed | accounting-capability-map.md |
| accounting | ~~`ai-suggest-vendor` macro has no UI — vendor field is a plain dropdown with no free-text auto-match~~ **CLOSED (2026-07-12, `975ceca9`)** — `BillsPanel`'s vendor field is now a debounced free-text combobox showing the macro's real token-overlap match/score or a create-new-vendor suggestion, never force-locked to only known vendors. | QuickBooks Online / Xero-class | ENGINEERING | "real gap... Small, contained addition — not attempted here" | accounting-capability-map.md |
| accounting | ~~`validate-ledger` macro (imported-books integrity checker) has no button in `AccountingActionPanel`~~ **CLOSED (2026-07-12, `975ceca9`)** — a 5th "Validate" action calls the real macro and renders its actual balanced/issue-list result, with an honest "no issues" state instead of a JSON dump. | QuickBooks Online / Xero-class | ENGINEERING | "reasonable small ENGINEERING follow-up" | accounting-capability-map.md |
| accounting | No multi-unit property / rent-roll management | QuickBooks Online / Xero-class | ENGINEERING (non-goal) | "arguably out of scope by design... **non-goal**" | accounting-capability-map.md |
| admin | ~~Ops-console backlog state (alert rules, feature flags, incident timeline) is in-memory, not persisted/replicated across shards~~ **CLOSED (2026-07-12, `5501dd98`)** — migration 364 adds `admin_alert_rules`/`admin_feature_flags`/`admin_incidents` (one shared row per rule/flag/incident, genuinely global/per-deployment); the domain reads/writes through an `alertRuleStore`/`featureFlagStore`/`incidentStore` db-or-memory facade (same pattern as `domains/education.js`/mig 363, `domains/tournaments.js`/mig 360), durable whenever `ctx.db` is present. `alertEvaluate` still evaluates firing state correctly reading rules from the DB-backed store. `series`/`tenants`/`logBuffer`/`traces` stay in-memory by design (high-churn telemetry, not restart-durable state). See `admin-capability-map.md` for the full scope rationale and `server/tests/admin-ops-persistence.test.js` for the persistence proof (raw-SQL + second-DB-handle checks). | Datadog / Grafana | ENGINEERING | closed — see `admin-capability-map.md` | admin-capability-map.md |
| agents | ~~`routeTask`'s `requiredSkills` input has no UI to author a task definition — ranking ignores skill filters~~ **CLOSED (2026-07-16, `06bffb64`)** — new `createTaskDefinition`/`listTaskDefinitions`/`deleteTaskDefinition` per-user CRUD family, cloned from the existing `createSchedule` family. `routeTask` now resolves an optional `taskDefinitionId` and uses its real `requiredSkills` to filter/rank candidates (inline array still works for back-compat). Proven load-bearing, not cosmetic: two identically-loaded candidates tie under the old always-empty-array behavior (75/75); a saved skill filter flips the #1-ranked agent to the genuine match (100 vs 50) — pinned by a dedicated ranking-flip test. New "Task Definitions" tab in `AgentRuntime.tsx` + a real filter dropdown next to Route Task. 10 new backend tests, 7 new frontend tests. | AutoGPT / CrewAI / Zapier-Agents | ENGINEERING | closed | agents-capability-map.md |
| alliance | ~~No direct-message primitive between external alliance members~~ **CLOSED (2026-07-16, `cccb92ce`)** — new `dmThreadKey`/`findAllianceMember`/`dmDisplayName` helpers + `dm-send`/`dm-list`/`dm-inbox` macros in `server/domains/alliance.js`. Recipient validation is genuinely different from `message-send`'s same-alliance-membership gate: `findAllianceMember` scans every alliance the system knows about, since a cross-org DM's whole point is reaching someone outside the caller's own alliance. `AllianceWorkspace.tsx` gets a Workspaces/Direct rail toggle + a real member picker (no free-text recipient box) + a threaded `DmPanel`. 25 new backend tests, 9 new frontend tests. | Slack Connect | ENGINEERING | closed | alliance-capability-map.md |
| alliance | ~~No message-search macro/UI across a shared alliance channel~~ **CLOSED (2026-07-12, `a18c2be0`)** — `server/domains/alliance.js`'s new `message-search` macro (content substring scan, `searchDtus`-shaped query bounds [2,200], membership-gated) plus a debounced search box in `AllianceWorkspace.tsx` with highlighted matches and an honest "No matches" state. See `alliance-capability-map.md` item #10 for the full writeup and test list. | Slack Connect | ENGINEERING | closed — see `alliance-capability-map.md` | alliance-capability-map.md |
| animation | No direct social-platform posting (TikTok/YouTube/Instagram/Discord) for exported clips | FlipaClip / Pencil2D | DATA-SOURCING | "would be a platform-wide connector, like the Gmail/Calendar work" | animation-capability-map.md |
| animation | ~~Public (logged-out) share-link viewing is blocked — `animation` isn't in `publicReadDomains`~~ **CLOSED (2026-07-16, `ad5d9d4c`):** widening `publicReadDomains` was rejected — that would open every animation macro, including mutating ones, to anonymous callers. Instead clones the welding client-portal pattern: a dedicated `GET /api/animation/share/:token` route that invokes the `animation.share-get` `LENS_ACTIONS` handler directly, bypassing `/api/lens/run`'s anonymous-caller gate. The action name is hardcoded in the route helper, never accepted as a request param. `share-get` already resolved the owner/animation purely from the unguessable token and already redacted frames when downloads are disallowed — no macro logic changed. Share page now fetches the route with no auth dependency; middleware/AppShell gained the new public prefix. 13 new backend E2E tests (incl. smuggled-action + 401-sanity checks) + 4 new frontend tests, 0 regressions. | FlipaClip / Pencil2D | ENGINEERING | closed | animation-capability-map.md |
| animation | ~~No full cross-operation undo/redo stack (only single-level per-layer stroke undo)~~ **CLOSED (2026-07-16, `15c04c2e`):** a bounded op-log (`AN_MAX_UNDO_DEPTH = 40`) lives in a separate side-map keyed by animId — deliberately off the animation object itself, since `anim-get` and other reads return that object straight to the client and putting snapshot history on it would have silently serialized up to 40 full-frame-tree clones into every load. All 8 destructive frame/layer macros push a deep-cloned snapshot before mutating; new `anim-undo`/`anim-redo` pop from one stack to the other with standard clear-redo-on-new-edit semantics. The pre-existing single-level `anim-stroke-undo` is untouched — the new stack composes with it rather than replacing it. `AnimStudio` gets a real Redo button and frame-count clamping on both undo and redo. 15 new backend tests (incl. an exact depth-cap boundary test) + 6 new frontend tests, 0 regressions (92/92 combined animation suite). | FlipaClip / Pencil2D | ENGINEERING | closed | animation-capability-map.md |
| animation | ~~No rotoscope-style photo-reference import onto a frame~~ **CLOSED (2026-07-16, `1bcccad7`)** — new `animation.frame-layer-import-image` macro attaches an already-uploaded reference image to a frame as a dedicated `type:"reference"` layer, never vectorized into strokes. All three stroke-commit macros now reject strokes on a reference layer server-side (not just by convention), and a latent `frame-duplicate` bug that would have dropped a reference layer's `type`/`imageRef` on copy is fixed. `AnimStudio.tsx` renders it as a real canvas underlay, visually distinct in the layer list; `AnimationReferenceImages.tsx`'s own header comment previously disclaimed this capability outright — it's rewritten to describe the real, honestly-scoped feature. 10 new backend tests, 8 new frontend tests. | FlipaClip / Pencil2D | ENGINEERING | closed | animation-capability-map.md |
| anon | ~~Differential-privacy epsilon-budget tracking only reflects the current call, no cross-session accumulation~~ **CLOSED (2026-07-16, `346320b2`)** — new `budgets` per-user state bucket (same idiom as `identities`/`conversations`); `differentialPrivacy` now spends its real per-call epsilon against the caller's persistent ledger, never accumulated on a failed/short-circuited call. New `privacyBudgetStatus`/`privacyBudgetReset` macros. Accumulation window is forever-until-explicit-reset by design — no product-defined epoch exists to anchor an arbitrary rolling-decay length to. New `PrivacyBudgetPanel.tsx` with a two-step confirm before reset. 11 new backend tests, 8 new frontend tests. | ARX / k-anonymity & DP tooling | ENGINEERING | closed | anon-capability-map.md |
| ar | No live physics simulation step in the viewport (fields authored/persisted, nothing steps them) | Adobe Aero / 8th Wall Studio | ENGINEERING | "out of scope for this rebuild (a new runtime dependency)" | ar-capability-map.md |
| ar | ~~No AR capture/screenshot/recording gallery~~ **CLOSED (2026-07-16, `c1a7b813`)** — new `captureUpload`/`captureList`/`captureGet`/`captureDelete` macros, a real 5MB/25MB size cap honestly rejecting oversized payloads (mirroring `photo-gallery.js`'s existing precedent). The capture is a genuine pixel-level grab from the real react-three-fiber WebGL canvas (`onCreated` + `preserveDrawingBuffer:true`) via `canvas.toDataURL()` for screenshots and `canvas.captureStream()`+`MediaRecorder` for real video/webm recording, with honest browser-support fallback messaging — never a placeholder image. New `ARCaptureGallery.tsx` mounted as a new "Capture" tab in `SceneStudio.tsx`. | Adobe Aero / 8th Wall Studio | ENGINEERING | closed | ar-capability-map.md |
| ar | No multi-user / collaborative AR sessions | Adobe Aero / 8th Wall Studio | ENGINEERING | "would require a new realtime layer, not a UI fix" | ar-capability-map.md |
| art | ~~`stroke-batch` (bulk-insert for importing/pasting many strokes) has no producing UI workflow~~ **CLOSED (2026-07-16)** — `ArtCanvas.tsx` now has a real copy/paste flow on the existing marquee/lasso selection: Copy snapshots the selected strokes' real data client-side; Paste serializes them through `art.stroke-batch`, offset 24px so the pasted copies are visibly distinct from the originals, then reloads so the server-minted copies render (Paste disabled with an empty clipboard — no wasted round-trip). `stroke-batch` was already fully covered server-side (`server/tests/art-domain-parity.test.js`, `server/tests/depth/art-behavior.test.js`) — this closed only the missing UI producer, no backend changes. 5 new frontend tests (`tests/components/ArtCanvasSymmetryClipboard.test.tsx`). | Procreate / Krita | ENGINEERING | closed | art-capability-map.md |
| art | ~~`symmetry-mirror-stroke` (live symmetry/mandala drawing mode) is real and unwired~~ **CLOSED (2026-07-16)** — guide state is lifted from `ProStudioPanel` into `ArtCanvas`, whose `commit()` now calls `art.symmetry-mirror-stroke` with the real committed strokeId whenever a mirror-shaped guide (vertical/horizontal/quadrant/radial) is active, persisting real mirrored strokes into the same layer and reloading so they render. Scoped honestly as post-commit mirroring of an already-persisted stroke, not true live-during-stroke rendering — the macro itself is designed post-commit (it takes a strokeId, not live point data), so a full live-preview-while-drawing mode remains separately scoped. `symmetry-mirror-stroke` was already fully covered server-side — this closed only the missing caller, no backend changes. 5 new frontend tests (`tests/components/ArtCanvasSymmetryClipboard.test.tsx`, shared with the row above). | Procreate / Krita | ENGINEERING | closed | art-capability-map.md |
| artistry | ~~No direct-messaging system between creators~~ **CLOSED (2026-07-16, `37d23815`):** structurally cloned from `alliance.js`'s cross-org DM primitive (`dmThreadKey`/`dm-send`/`dm-list`/`dm-inbox`) — same sorted-pair threadKey so both sides converge on one storage key. Message shape mirrors this lens's own simpler comment shape rather than importing alliance's richer attachments/reactions shape; recipient validation adapts from alliance's closed-membership scan to an open-participation check (has this userId set a profile or created a project) since artistry has no membership concept. Privacy enforced structurally — `dm-list` derives its key from the caller's own id, so a third party can never land on two other users' real thread key. New Message action on following/follower rows + an `ArtistryDmPanel`. 20 new backend tests (incl. an explicit privacy-scoping test) + 7 new frontend tests, 0 regressions (62/62 combined artistry suite). | Behance/ArtStation | ENGINEERING | closed | artistry-capability-map.md |
| artistry | ~~No native image upload/blob-storage pipeline for project images (URL-only)~~ **CLOSED (2026-07-16, `08056272`):** deliberately NOT built on the misfiled `apiHelpers.artistry.blobs` facility in `server.js` (a different, cross-lens DAW audio-blob system whose own download route only ever returns metadata, never the bytes) — a wholly separate artistry-native store, cloned from `travel.js`'s binary attachment trio (base64 validation, an 8MB cap, blob never returned outside the dedicated download macro). Uploaded images are referenced via a stable `artistry-img:<id>` scheme; `projectCreate`/`projectUpdate` validate any such reference actually belongs to the calling user before accepting it, silently dropping a foreign or fabricated ref. External URLs keep working unchanged. `ProjectStudio` gains a real file-input alongside the existing free-text textarea and a `ResolvedImage` component that transparently resolves references at every `<img>` render site with an honest loading/failure fallback. 19 new backend tests + 10 new frontend tests, 0 regressions (51/51 combined artistry suite; 7/7 pre-existing `artistry-lens-states`). | Behance/ArtStation | ENGINEERING | closed | artistry-capability-map.md |
| artistry | ~~No creator analytics trends (point-in-time totals only)~~ **CLOSED (2026-07-16, `951ea868`)** — new `analyticsSnapshot`/`analyticsHistory` macros, sharing a single `computeArtistryStats()` source of truth with `profileGet` so a trend point always matches the day's real totals. One row per (user, UTC day) — a same-day repeat updates in place rather than accumulating duplicates. `profileGet` auto-captures today's snapshot on owner load (never on a visitor's), best-effort so a snapshot failure can never break profile loading. New `AnalyticsTrendChart.tsx` reuses the existing shared `ChartKit`, no new charting dependency. | Behance/ArtStation | ENGINEERING | closed | artistry-capability-map.md |
| artistry | ~~No notification feed (new follower/comment/appreciation)~~ **CLOSED (2026-07-16, `e8b9e207`):** wired into the existing `social-layer.js` notification substrate (`createNotification`/`getNotifications`) already used elsewhere in the codebase — no parallel notification system invented. `follow` fires only on a genuine new-follow transition, `commentAdd`/`appreciate` fire only on real state changes and never self-notify. New `notifications-list` (type-scoped to follow/comment/like) and `notifications-mark-read` macros; new `ArtistryNotifications.tsx` reuses the existing `useSocialNotificationToast` tone mapping. 19 new backend tests + 8 new frontend tests, 0 regressions (113/113 combined artistry backend suite). | Behance/ArtStation | ENGINEERING | closed | artistry-capability-map.md |
| artistry | Shared DAW/marketplace/studio backend misfiled under `/api/artistry/*` namespace | none stated | ENGINEERING | "scoped future backend-scoped fix... out of scope for a frontend-only lens rebuild" | artistry-capability-map.md |
| astronomy | No live weather-webcam / seeing-cam integration | Stellarium/SkySafari | DATA-SOURCING | "no free API identified with acceptable ToS" | astronomy-capability-map.md |
| astronomy | No multi-observer shared/co-observing session | Stellarium/SkySafari | ENGINEERING | "would need a new realtime substrate" | astronomy-capability-map.md |
| atlas | ~~No edit-in-place UI for a saved place (`places-update` unsurfaced)~~ **CLOSED (2026-07-12, `f4a90d4e`)** — `PlacesPanel` pencil-icon edit form calls `places-update` with the changed fields | Google Maps | ENGINEERING | "small scoped build ~40 LOC" | atlas-capability-map.md |
| atlas | ~~No drag/reorder UI for trip stop sequence (`trips-reorder-stops` unsurfaced)~~ **CLOSED (2026-07-12, `f4a90d4e`)** — `TripsPanel` up/down chevron buttons call `trips-reorder-stops` with the full reordered `stopIds` array | Google Maps | ENGINEERING | "scoped build ~30 LOC" | atlas-capability-map.md |
| atlas | No 3D-volume viewer for signal-tomography data | InSAR ground-deformation platforms | DATA-SOURCING | "store is structurally empty, no mesh-network ingestion pipeline" | atlas-capability-map.md |
| atlas | No material/subsurface classification UI for signal tomography | InSAR platforms | DATA-SOURCING | "empty substrate makes it low-value right now" | atlas-capability-map.md |
| atlas | No displacement/change-over-time map (backend `change` macro is deliberately unwired sim-only) | InSAR platforms | ENGINEERING | "correct fix is a backend voxel-diff implementation, out of scope" | atlas-capability-map.md |
| atlas | ~~No ad-hoc spatial query UI (`query` macro)~~ **CLOSED (2026-07-16, `a408ad2a`):** pure frontend-wiring gap — `executeSpatialQuery` (`foundation-atlas.js`), the `atlas.query` macro, and `POST /api/atlas/query` were all already real and rate-limited; no backend change needed. New `SpatialQueryForm` mirrors the backend's own six-case dispatch (point/area/radius/material/subsurface/changes) with a real per-type field set, reading the type list straight from the backend's own `validTypes` error payload. This deployment's mesh substrate is structurally empty, so a well-formed query honestly returns zero results — the same disclosure pattern already established by this page's `classify`/`privacy_zones` tabs. 20 new frontend tests, 0 regressions (34/34 combined atlas-forms suite). | InSAR platforms | ENGINEERING | closed | atlas-capability-map.md |
| atlas | ~~No manual signal-classification submission form~~ **CLOSED (2026-07-12, `336cbc95`)** — `SignalClassifyForm` (frequency/modulation/lat+lng/bandwidth/power/description/keywords) wired into the Signal Tomography "Signals" tab, calling `cortex.classify` with server-side validation added for `frequency` + `origin`; invalidates the taxonomy/spectrum/anomalies queries on success. `server/tests/depth/atlas-signal-classify-behavior.test.js` (14/14) + `concord-frontend/tests/components/atlas/SignalClassifyForm.test.tsx` (5/5) | InSAR platforms | ENGINEERING | "highest-value future build... well under 150 LOC" | atlas-capability-map.md |
| atlas | ~~No privacy-zone map/verification UI~~ **CLOSED (2026-07-16)** — new "Privacy" tab in Signal Tomography mode (`app/lenses/atlas/page.tsx`) reuses `AtlasPrivacyMonitor` (previously orphaned on the chat lens) for a real zones list + stats view against `GET /api/atlas/privacy_zones?view=zones\|stats\|verify` (new `apiHelpers.atlasTomography.privacyZones` client), plus a new `PrivacyVerifyForm` that verifies a zone's interior-never-generated guarantee by real zone id picked from the fetched list (never free-typed). Honest disclosure banner: like Signal Tomography's other tabs, this deployment has no automatic zone-detection pipeline calling `detectPrivacyZone` yet, so the list is real and honestly empty until zones exist. `concord-frontend/tests/components/chat/AtlasPrivacyMonitor.test.tsx` (7/7) + `concord-frontend/tests/components/atlas/PrivacyVerifyForm.test.tsx` (5/5) | InSAR/RF-tomography platforms | ENGINEERING | "a full zone-map UI is a multi-panel build" | atlas-capability-map.md |
| atlas | No business reviews/ratings on place-detail pages (no OSM data source) | Google Maps | DATA-SOURCING | "No OSM-sourced review data exists to surface; not fabricated" | atlas-capability-map.md |
| automotive | ~~No dashboard "upcoming renewals" widget (`renewals-upcoming` unsurfaced)~~ **CLOSED (`1e2b64a1`)** | Drivvo/Fuelly/CARFAX Car Care | ENGINEERING | "documented, real, non-urgent gap" | automotive-capability-map.md |
| aviation | NOTAM fetch gated behind paywalled `FAA_NOTAM_API_KEY` tier | ForeFlight/Garmin Pilot/Jeppesen | DATA-SOURCING | "explainable (paywalled tier), not a defect" | aviation-capability-map.md |
| aviation | ~~No edit form for an already-created aircraft profile (`aircraft-update` unsurfaced)~~ **CLOSED (`1e2b64a1`)** | ForeFlight/Garmin Pilot/Jeppesen | ENGINEERING | "Minor, left for a future pass" | aviation-capability-map.md |
| byo-keys | ~~No per-key rate limiting (only monthly cap exists)~~ **CLOSED (2026-07-12, `8d76c3a8`)** — new `server/lib/byo-rate-limit.js` is a real continuous-refill token bucket per (userId, slot), matching the same bucket algorithm `server/lib/socket-rate-limit.js` already uses for socket-flood protection, generalized to a requests-per-minute cadence. `server/lib/byo-router.js#brainChat` — the actual outbound-call dispatch chokepoint for every BYO-key inference (chat, expert-mode, reason.verify, agent-marathon, maker-checker, llm.local) — calls `consumeRateLimitToken(userId, slot)` at the top of the override branch, BEFORE the key is decrypted or the provider is contacted, and returns an honest `{ ok:false, error:'rate_limited', retryAfterMs }` when the bucket is empty; fail-open (never blocks) when no limit is configured, matching `budget_check`'s existing "no cap = unlimited" convention. Two new macros, `byo_keys.rate_limit_set`/`byo_keys.rate_limit_status`, mirror `set_budget`/`budget_status`'s naming and call the exact same lib functions the router enforces with (not a parallel implementation that could drift). New bespoke `RateLimitPanel.tsx` (token-bucket gauge + live poll, sits beside `BudgetPanel` in the lens) — this is a separate, orthogonal control from the monthly $/token ceiling, capping burst rate rather than spend. Tests: `server/tests/byo-rate-limit.test.js` (19/19, pure token-bucket + macro contract, clock-injected — no real sleeps), `server/tests/byo-rate-limit-router.test.js` (5/5, exercises the real `brainChat()` call path end-to-end with a stubbed global `fetch` proving a blocked call never reaches the network), `concord-frontend/components/byo-keys/RateLimitPanel.test.tsx` (7/7). See `byo-keys-capability-map.md` checklist item #9 for the full writeup. | OpenRouter / LiteLLM / Vercel AI Gateway | ENGINEERING | closed — see `byo-keys-capability-map.md` | byo-keys-capability-map.md |
| byo-keys | ~~No proactive spend alerts at a % budget threshold~~ **CLOSED (2026-07-12, `e3094f2a`)** — new `byo-budget-alert-cycle` heartbeat (freq 20, scope global) sweeps every user's budgets each ~5min via `checkSpendAlerts()` (once-per-crossing dedupe at 80%/100%) and dispatches a real, live in-app toast through the existing `createNotification`/`social:notification` channel already used by likes/comments/mentions — no new delivery mechanism invented, per the doc's own "in-app, not email/push" scoping. See `byo-keys-capability-map.md` checklist item #10 for the full writeup, including an honestly-flagged pre-existing (unrelated, out-of-scope) bug found while verifying the channel: the persistent notification bell/center reads from a `STATE.notifications` Map nothing ever writes to, so only the real-time toast is confirmed reliable today, not a durable catch-up record. Tests: `server/tests/byo-budget-alert-cycle.test.js` (14/14). | OpenRouter / LiteLLM / Vercel AI Gateway | ENGINEERING | closed — see `byo-keys-capability-map.md` | byo-keys-capability-map.md |
| calendar | ~~Main event grid persists through a generic artifact-CRUD store, disconnected from the real scheduling engine~~ **CLOSED (`66b40daa`)** — rewired onto the real `calendar.calendars-*`/`events-*` macros, newly surfacing live conflict detection + multi-calendar filtering. | Google Calendar/Calendly | ENGINEERING | "a real seam worth unifying in a future pass" | calendar-capability-map.md |
| board | Two independent, real kanban implementations remain unconsolidated | Trello | ENGINEERING | "recommendation for a future dedicated unit" | board-capability-map.md |
| careers | ~~No UI shows a track's full 10-tier wage/reputation ladder (`ladder` macro unsurfaced)~~ **CLOSED (`1e2b64a1`)** | Stardew Valley / Upwork | ENGINEERING | "a ladder preview under the track select" | careers-capability-map.md |
| careers | ~~No way to originate a new contract offer to an NPC/employer~~ **CLOSED (2026-07-12, `0b9fcd40`)** — new `careers.employers` macro (`server/lib/career-employers.js`, a READ-ONLY archetype→track directory over `world_npcs`) + `<EmployerBrowser>` propose-contract UI calling the real `careers.offer` macro. | Stardew Valley / Upwork | ENGINEERING | "a larger scoped build" | careers-capability-map.md |
| careers | ~~Player's own reputation number/tier-gate consequence never rendered~~ **CLOSED (2026-07-12, `0b9fcd40`)** — new `careers.myReputation` macro (`server/lib/career-contracts.js#deriveWorkerReputation`, using the SAME `reputationGateTier`/`reputationWageMultiplier` `offerContract` enforces) + `<ReputationGate>` UI, also now used to compute `careers.offer`'s reputation gate server-side instead of trusting a client-supplied value. | Stardew Valley / Upwork | ENGINEERING | "surfacing gap only" | careers-capability-map.md |
| chat | ~~Server-backed conversation branch/fork has a fully-built, tested component that's never mounted~~ **CLOSED (`58dd1ea6`)** — `BranchForkButton` mounted. | Claude.ai / ChatGPT | ENGINEERING | "deserves its own scoped pass, not a quick insert" | chat-capability-map.md |
| civic-bonds | ~~No way to view restricted per-scope spillover fund balance~~ **CLOSED (2026-07-12, `f4aa2297`)** — the "no natural per-scope selector" blocker is resolved by a new `civic_bonds.scopes` macro that returns the real, canonical `GOVERNANCE_SCOPES` hierarchy (town/city/county/state/national/international — the same list migration 305's `civic_bonds.scope` column comment already pointed at, reused read-only from `server/emergent/microbond-governance.js`, not invented). The lens now fetches it, renders a real `<select>` scope picker defaulting to "city", and queries `civic_bonds.spillover` per selection, showing the true restricted-fund balance (including honest `0`/`Unavailable` states) — never a disconnected number. See `civic-bonds-capability-map.md` item 7 for the full writeup and test list. | Kickstarter / municipal bond portals | ENGINEERING | closed — see `civic-bonds-capability-map.md` | civic-bonds-capability-map.md |
| code | ~~Three sub-systems each maintain independent "current project" picker state instead of one shared context~~ **CLOSED (2026-07-12, `220fd971`)** — new `CodeProjectContext`/`useCodeProject` (`concord-frontend/components/code/CodeProjectContext.tsx`) is now the single source of truth for `projectId`, provided once in `app/lenses/code/page.tsx` and consumed by `CodeWorkbenchSection`'s `ProjectSwitcher`, `CodeAdvancedPanel`'s previously-disconnected `ProjectSwitcher`, and a new `QuickScriptProjectBadge` in the quick-script header (the quick-script tabs' ephemeral `code-lens-live` LSP-sync buffer is deliberately left untouched — repointing it at a real project would risk silently overwriting real files, exactly the risk this item was originally deferred over). Presentation-only, no backend changes. See `code-capability-map.md` for the full writeup + test list. | VS Code / Copilot Chat / Cursor | ENGINEERING | closed — see `code-capability-map.md` | code-capability-map.md |
| codex | ~~No dedicated per-id lore-entry fetch/permalink~~ **CLOSED (2026-07-12, `9f7f4d8e`)** — `lore.get` was already real (verified before writing any code — 17/17 `server/tests/codex-lens-macros.test.js` unchanged, no backend edit needed); the gap was purely the frontend never calling it. `/lenses/codex?id=<loreId>` now resolves that entry via `lore.get` in a dedicated detail dialog (works regardless of active browse filters), and every entry (plus the dialog) gets a "Copy permalink" control so the link is producible, not just consumable. See `codex-capability-map.md` for the full writeup and test list. | Destiny 2 Lore Book / Hades Codex | ENGINEERING | "only if a shareable-permalink feature is ever prioritized" | codex-capability-map.md |
| collab | ~~No read-only "who's viewing" observer roster surface~~ **CLOSED (2026-07-16, `ac77433f`):** an eye-icon "peek" affordance next to each row in `CollabDocWorkspace`'s doc list polls the already-real `collab.presenceState` macro on a short interval, showing who's currently viewing a document without opening it. Deliberately never calls `openDoc`/`cursorUpdate` — peeking never adds the peeker's own cursor to that doc's presence map (confirmed: `presenceState` only reads `s.presence`; only `cursorUpdate` writes to it). No backend change needed — `presenceState` was already the correct read-only primitive, just unsurfaced. Honest empty state ("No one else viewing right now.") when nobody's there. 2 new backend tests (`presenceState` never mutates presence, safe no-op on zero viewers) + 6 new frontend tests (correct macro call, honest empty state, polling starts/stops cleanly). | Discord/Teams-style session rooms | ENGINEERING | closed | collab-capability-map.md |
| collab | ~~No live participant join/leave with real roster sync~~ **CLOSED (2026-07-12, `7468a72b`)** — `collab.sessionJoin`/`collab.sessionLeave`/`collab.sessionRoster` macros (`server/domains/collab.js`) now track a genuine live-joined set per session, broadcast over `collab:${sessionId}` via new `collab:participant-joined`/`collab:participant-left` realtime events (registered in `server/lib/event-shapes.js`), and the frontend's "Join" flow (`concord-frontend/app/lenses/collab/page.tsx`'s `ActiveSessionView`) calls the real macro on mount/unmount instead of only opening the socket room. See `collab-capability-map.md`'s roster-sync section for the full fix + regression-test detail. | Discord/Teams-style session rooms | ENGINEERING | closed — see `collab-capability-map.md` | collab-capability-map.md |
| collab | ~~No producer for the Invitations tab (pairwise DM shape differs from open sessions)~~ **CLOSED (2026-07-16, `128f214b`)** — new `sessionInvite`/`sessionInviteRespond`/`sessionInviteList` macros, in a dedicated `STATE.collabLens.invites` store scoped per-caller (the generic cross-user artifact store the frontend previously targeted would have made a private invite readable by any caller, since `collab` is a "social" lens domain). `sessionJoin` was refactored into a named `sessionJoinHandler` so accepting an invite calls the exact same join logic rather than duplicating it. Untangled two other unrelated substrates in the process (the pairwise `COLLAB_SESSIONS` HTTP route and a `/api/shared-session/*` click-through) and left both untouched. | Discord/Teams-style session rooms | ENGINEERING | closed | collab-capability-map.md |
| command-center | No real on-call paging (SMS/phone/push) — `onCall` is a free-text note | PagerDuty | DATA-SOURCING | "would need an external connector (Twilio/webhook)... deliberately deferred" | command-center-capability-map.md |
| command-center | No multi-user/team on-call rotation or shared incident visibility (per-operator by design) | PagerDuty | ENGINEERING | "Scoped future build if multi-operator visibility is ever wanted" | command-center-capability-map.md |
| command-center | ~~`saveDashboard` stores a panel-id list, not a live composable widget grid~~ **CLOSED (2026-07-16, `cc55e66f`)** — new `dashboardData` macro resolves each saved widget id against real sources: a matching vital metric (via a `computeVitalHistory` helper extracted verbatim from `vitalHistory`, zero behavior change confirmed by the unmodified domain-parity suite) or a matching alert rule. A widget matching neither reports an honest per-widget `{id, error}` instead of a fabricated chart, and one bad widget never breaks the rest of the response. New `DashboardWidgetTile` grid in `OpsCockpit.tsx` reuses the existing `ChartKit` — no new charting dependency. 7 new backend tests, 5 new frontend tests. | Datadog / Grafana | ENGINEERING | closed | command-center-capability-map.md |
| consulting | Generic DTU artifact-library tier duplicates the "Engagement" concept alongside the real `EngagementTracker` | Bonsai / Harvest / Deltek | ENGINEERING | "future Wave-4 pass could consolidate or clearly re-label" | consulting-capability-map.md |
| council | ~~`council.simulate-budget` (variance-weighted budget projection) has zero frontend caller~~ **CLOSED (`350f2dae`)** | Loomio + Convene/BoardEffect/OnBoard | ENGINEERING | "additive net-net UI... deferred to a future pass" | council-capability-map.md |
| council | ~~`council.audit`'s derived process-completeness trail is computed but never surfaced~~ **CLOSED (`350f2dae`)** | Loomio + Convene/BoardEffect/OnBoard | ENGINEERING | "a future pass could add the derived trail as a secondary card" | council-capability-map.md |
| courtship | ~~No manual "end this marriage" action (only automatic estrangement path exists)~~ **CLOSED (2026-07-12, `7bf37520`)** — new `ConfirmDissolveModal` (`concord-frontend/components/courtship/ConfirmDissolveModal.tsx`) gates a real "End Marriage" button on each active-marriage row behind a confirm-then-commit step (matching the `HeartEventModal` dialog idiom already in this lens); confirming calls the real `courtship.dissolve` macro, a dissolved marriage disappears from the active list and now surfaces in a new "Past marriages" section sourced from `courtship.marriages{activeOnly:false}`. Also closed a real authz gap found while wiring this: `courtship.dissolve`/`romance.dissolve` took no `ctx.actor.userId` at all — any authenticated caller who knew a `marriageId` could dissolve someone else's marriage. `romance-engine.js#dissolveMarriage` now takes an optional `expectedUserId` and both macros pass the caller's id, rejecting with `not_a_party` when the marriage belongs to someone else. See `courtship-capability-map.md` for the full closure writeup + test list. | none stated | ENGINEERING | closed — see `courtship-capability-map.md` | courtship-capability-map.md |
| creator | ~~Tier pricing on a marketplace listing is stored but never enforced at purchase time (no purchase route; 5 parallel listing stores exist platform-wide)~~ **PARTIALLY CLOSED (2026-07-12, Wave 4 gap-closure unit)** — the "no purchase route" half is fixed: the Creator lens's Listings tab (create/edit/withdraw/relist) now reads and writes the real `dtu.marketplace` + `purchaseWithRoyalties` store (95%/5% split, royalty cascade, already live via `PurchaseButton`/`TrackCard`/crafting+music lenses) instead of the dead `STATE.marketplaceListings` map, via 4 new small macros (`marketplace.myListings`/`updateListing`/`unlist`/`relist`) plus an ownership+scope gate added to the pre-existing `marketplace.list` (it had none — any authenticated caller could previously flip any dtuId, including one they didn't own, into a listing; dormant until this pass gave it a real frontend caller). `computeCreatorDashboard`/`computeReputationLeaderboard` were also extended to read the same real store so the Overview tab's stat tiles and the leaderboard stay coherent with the Listings tab on the same page. Tier-pricing enforcement itself is NOT fixed — `tierPrices` remains stored-but-unenforced metadata in the new store too, same status as before, just relocated to a store that's actually purchasable; a real fix needs a deliberate license-tier design decision, not a silent default. Also corrected during investigation: the store count was undercounted at "5" — re-audit found at least 7 parallel listing/marketplace subsystems (`STATE.marketplaceListings`, `dtu.marketplace`+`purchaseWithRoyalties`, `PLUGIN_MARKETPLACE`, `STATE.economic.listings`, the DB-backed `creative_artifacts` system, `STATE.marketplaceLens` Etsy-style shop/listings/orders in `domains/marketplace.js`, and a separate DB-backed listings/purchase/entitlement system in `server/durable.js`). `creative_artifacts` was evaluated as the alternate real-store candidate and rejected as the redirect target: it requires a real uploaded file (not just a DTU record) and a full per-content-type tier ladder validated by exact tier-id membership (e.g. MUSIC needs listen/download/remix/commercial/exclusive/stems all priced) — the Creator lens's existing `{usage,remix,commercial}` shape fails that validation outright, so adopting it would have meant a UI rebuild, not a redirect. | YouTube Studio + Buffer + Patreon | ENGINEERING | "touches money flow and multiple call sites... flagged for a dedicated follow-up" | creator-capability-map.md |
| creatures | No creature portrait/thumbnail imagery (procedural body-plan system, no 2D art pipeline) | Spore's Creature Creator / Pokédex-style lab | ENGINEERING | "correctly left undone rather than faked with stock art or emoji" | creatures-capability-map.md |
| creatures | ~~No global keyboard-shortcut registration (`useLensCommand`)~~ **CLOSED (2026-07-16):** `app/lenses/creatures/page.tsx` now registers 4 shortcuts via `useLensCommand` — `r` refresh, `/` focus codex search, `l` focus lineage input, `b` breed (enabled only when both parents are picked and not already breeding) — each bound to the real pre-existing `refresh`/`breed`/`pickA`/`pickB` handlers, not new logic. Discoverable via `<kbd>` chips next to the Refresh/Breed buttons and input placeholder hints, per the UI-quality-rubric discoverability requirement. 12 new frontend tests, all passing; no regressions. | none stated | ENGINEERING | closed | creatures-capability-map.md |
| daily | Shared `generateWaveform()` used platform-wide is itself a synthetic waveform generator (unlike daily's own fixed real-PCM decode) | Day One / Reflectly | ENGINEERING | "a shared-infra change out of this unit's scope" | daily-capability-map.md |
| deities | ~~Full 200-row pilgrim log has no UI beyond the existing 50-row roster~~ **CLOSED (2026-07-12, `cef98c3a`)** — the `deity.pilgrim_log` macro was already real and correctly shaped (`{deityId, limit}` in, up to 200 rows out, verified against source, no backend change needed). `DeityDetailPanel.tsx` now offers a "view full pilgrim log" button that appears only when the existing 50-row roster is plausibly capped (the deity's real `pilgrim_count` exceeds the roster length), fetches the full log on click with honest loading/error states, and offers a collapse-back-to-50 control. Tests: 5 new (expand-button gating, exact macro-call params, full-list render, honest failure with no fabricated rows, zero-pilgrim honest empty state) + 6 pre-existing sibling tests unaffected, 11/11 re-run. | none stated | ENGINEERING | closed | deities-capability-map.md |
| desert | ~~No wildlife tracking / species catalog — no backend macro exists~~ **CLOSED (2026-07-16, `05702c1f`)** — new `sightingSave`/`sightingList`/`sightingDelete`/`sightingsNearby` macros in `server/domains/desert.js`, modeled on the file's own `nodeSave`/`nodeList`/`nodeDelete`/`nodesNearby` geolocation CRUD shape (`STATE.desertLens` Maps, shared `actor`/`nextId`/`nowIso`/`userMap`/`haversineKm` helpers). Real per-user sighting log: species (required), count, lat/lng, observation date (distinct from record-creation time), behavior, a soft-fallback confidence enum (certain/probable/possible), and an optional caller-supplied photo reference — never fabricated. Replaces the previous fabricated "Wildlife" tab (a stat card that mislabeled a count of resource nodes as "Species Cataloged"). New `WildlifeSightingLog.tsx` panel with a real confidence `<select>`, species breakdown, and a proximity search. Tests: 14 of the unit's 28 new backend cases + 8 new frontend cases. | Rockd/Mindat-style field app | ENGINEERING | closed | desert-capability-map.md |
| desert | ~~No infrastructure/hazard incident reporting~~ **CLOSED (2026-07-16, `05702c1f`)** — new `incidentReport`/`incidentList`/`incidentUpdateStatus`/`incidentDelete`/`incidentsNearby` macros, same geolocation-CRUD precedent as the sighting log above but with a genuinely distinct concept: this is a dated, categorized incident with a real status lifecycle (`open`→`investigating`→`resolved`), NOT a rehash of `nodeSave`'s existing `kind:"hazard"` static map marker (confirmed this distinction holds before building — a hazard node is a standing, permanent-until-deleted marker; an incident report is a specific dated filing with a lifecycle). `category` (10 real field-hazard categories) and `severity` are hard-rejected on an invalid value — deliberately not soft-defaulted like `nodeSave`'s tolerant-fallback style, since a mis-categorized safety incident is a real honesty concern. Resolving requires non-empty `resolutionNotes`; leaving `resolved` requires an explicit `reopen:true`. `incidentsNearby` surfaces `openCount`/`criticalCount` (excluding resolved incidents) as the genuine safety differentiator from a static pin. New `IncidentReportPanel.tsx` with real category/severity selects, color-coded status badges, a resolve-requires-notes gate, and prominent open/critical stat tiles in the nearby view. Tests: 14 of the unit's 28 new backend cases + 10 new frontend cases; 66/66 combined desert backend suite (0 regressions). | none stated | ENGINEERING | closed | desert-capability-map.md |
| desert | ~~No offline map caching for no-signal fieldwork~~ **CLOSED (2026-07-16, `06fa2e2c`):** new `lib/desert/tile-cache.ts` — browser Cache-API-backed tile store (tiles are opaque Request/Response-shaped PNG blobs, the exact shape Cache API models; a JSON manifest inside the same cache tracks byte-size/timestamp for LRU eviction, 50MB/4000-tile caps). New `DesertOfflineMapView.tsx` intercepts tile requests via a MapLibre `desert-tile://` custom protocol resolving through the cache, drawing an honest "tile unavailable offline" placeholder rather than a broken image — never touches the shared `MapView.tsx` (~25 other lenses use it unmodified) or `public/sw.js`. `ResourceNodeMap`/`TerrainOverlay`/`ExpeditionPlanner` render through it instead. 24 new frontend tests, 0 regressions. | none stated | ENGINEERING | closed | desert-capability-map.md |
| dtus | ~~`dtu_portability` (export/validate/import corpus backup) has zero frontend callers~~ **CLOSED (`b31200ea`)** — Backup & Portability panel built. | Obsidian/Roam (implicit) | ENGINEERING | "belongs to a future dedicated settings/backup surface" | dtus-capability-map.md |
| dtus | ~~Saved views + layer-edit overlays persist only to process-memory Maps, lost on restart~~ **CLOSED (2026-07-12, `2232d744`)** — migration 361 adds real `dtu_saved_views` + `dtu_layer_overlays` tables; `server/domains/dtus.js`'s 5 affected macros (`saveView`/`listViews`/`deleteView`/`getLayers`/`updateLayers`) now read/write through a db-or-memory store facade (same pattern as `domains/tournaments.js`/mig 360 and `domains/saved.js`/mig 356), durable whenever `ctx.db` is present, falling back to the legacy in-memory Maps only for minimal/test builds with no DB handle. All 5 macros' response shapes unchanged; the file's other macros (compare/merge, citation graph, etc.) untouched. See `dtus-capability-map.md` for the table schema and `server/tests/dtus-persistence.test.js` for the persistence proof (raw-SQL + second-DB-handle checks). | Obsidian/Roam | ENGINEERING | closed — see `dtus-capability-map.md` §3 for the before/after | dtus-capability-map.md |
| dx-platform | No native GitHub/GitLab PR-check integration | SonarQube/SonarCloud | ENGINEERING | "scoped future task" | dx-platform-capability-map.md |
| dx-platform | ~~No SARIF / standard interop export for findings~~ **CLOSED (2026-07-16, `29112655`)** — new `exportSarif` macro transforms `reviewDiff`'s real findings into a genuinely well-formed SARIF 2.1.0 document; the severity→level mapping reuses `ciGateCheck`'s own existing thresholds rather than inventing one, and `tool.driver.rules` is correctly deduplicated to one entry per distinct `detectorId` so every `results[].ruleId` resolves. `DxWorkbench.tsx` gets an "Export SARIF" button reusing the on-screen findings and the project's existing `downloadFile` helper. 11 new backend tests, 5 new frontend tests. | SonarQube/SonarCloud | ENGINEERING | closed | dx-platform-capability-map.md |
| dx-platform | ~~No historical issue-trend / "new vs. existing" tracking (leak period)~~ **CLOSED (2026-07-16, `5929ed82`):** migration 365 (`dx_finding_history`) — minimal commit-scoped provenance, not a findings warehouse: one row per (user, codebase, commit), upserted by `commitSha`. `reviewDiff` persists a snapshot only when the caller supplies `commitSha`; omitting it is byte-identical to before. New `issueTrend` macro reads the last two snapshots and set-diffs finding identity (`detectorId:path:line`) into new/existing/resolved counts — Sonar's leak period at its simplest, honestly empty with 0 or 1 snapshot. `DxWorkbench` gains a commit-SHA input + a Trend card rendered only when a SHA was supplied. 10 new backend tests + 3 new frontend tests, 0 regressions (48/48 combined dx-platform suite). | SonarQube's "leak period" | ENGINEERING | closed | dx-platform-capability-map.md |
| dx-platform | `dx.list_shadows` has no UI (blocked on plugin never calling `upsertShadow`) | none stated | ENGINEERING | "Revisit once concord-vscode actually calls upsertShadow" | dx-platform-capability-map.md |
| dx-platform | ~~`dx.get_weight`/`dx.weighted_findings` unsurfaced~~ — **CLOSED (STALE, already fixed)** (commit `543adb1f`, "Wave 4: wire dx-platform weight lookup, hypothesis history, integrations due/verify", independent of this ledger's own execution batches): `SeverityWeightsPanel.tsx` calls both `lensRun('dx', 'get_weight', ...)` and `lensRun('dx', 'weighted_findings', ...)`, confirmed real and live on inspection | none stated | ENGINEERING | closed | dx-platform-capability-map.md |
| eco | `sustainabilityScore` (corporate ESG scoring) has no fit in a personal ecology-tracker lens | none stated | ENGINEERING | "a future 'Eco for Organizations/Teams' surface" | eco-capability-map.md |
| education | ~~Courses/discussions/cohorts are per-user Maps, not a shared multi-tenant catalog~~ **CLOSED (2026-07-12, `9aac6a7a`)** — migration 363 adds `edu_courses`/`edu_discussions`/`edu_cohorts` (author_id-keyed, shared rows); the domain reads/writes through a db-or-memory store facade (same pattern as `domains/tournaments.js`/mig 360), durable whenever `ctx.db` is present. Both cross-user visibility AND cross-restart durability are solved. `courses-list`/`courses-search` now return every published course from every author plus the caller's own drafts; new `courses-update` macro + the existing `courses-delete`/`lessons-create` are ownership-gated (`authorId === caller`); discussions/cohorts are genuinely shared/open, with `cohorts-set-status` gated to the scheduling author. See `education-capability-map.md` for the table schema, the draft/publish disposition, and `server/tests/education-catalog-persistence.test.js` for the persistence proof (raw-SQL + second-DB-handle checks). | Khan Academy + Coursera | ENGINEERING | closed — see `education-capability-map.md` | education-capability-map.md |
| emergency-services | No multi-agency / mutual-aid interop | Tyler/CentralSquare CAD | ENGINEERING | "would need a cross-tenant substrate" | emergency-services-capability-map.md |
| emergency-services | No licensed CAD-grade map tiles / AVL hardware integration | Tyler/CentralSquare CAD | DATA-SOURCING | "structural (needs a paid data source)" | emergency-services-capability-map.md |
| emergency-services | No recorded 911 audio / CAD-to-RMS handoff | Tyler/CentralSquare CAD | DATA-SOURCING | "no free API identified with acceptable ToS" | emergency-services-capability-map.md |
| energy | No direct hardware CT-clamp / smart-plug pairing | Sense/Emporia Vue | ENGINEERING | "software-only by design" | energy-capability-map.md |
| energy | No automated device scheduling against live rates | Sense/Emporia Vue | ENGINEERING | "would need an automation substrate Concord doesn't have" | energy-capability-map.md |
| engineering | ~~`boltedConnection`/`transformerSizing` real AISC/ANSI math unreachable at the macro layer~~ **CLOSED (Wave 4 gap-closure)** — two new thin-passthrough macros, `engineering.connectionCheck` and `engineering.transformerSizing` (`server/domains/engineering.js`), call the real compute functions and return their real output (or an honest `{ok:false}` on invalid input); `MultiDisciplineCalcPanel.tsx` gained a matching "Bolted connection" sub-card (StructuralSection) and "Transformer sizing" sub-card (ElectricalSection), each with real inputs and its own Compute button. Pinned by `server/tests/depth/engineering-connection-transformer-behavior.test.js` (11 tests) and `concord-frontend/tests/components/MultiDisciplineCalcPanel.test.tsx` (4 tests). | MechaniCalc/Engineering ToolBox | ENGINEERING | "Out of scope for a frontend-audit pass" (superseded — closed in a dedicated backend+frontend unit) | engineering-capability-map.md |
| entity | ~~`entity.terminal_approve` (council-vote approval queue) has no listing mechanism/UI~~ **CLOSED (`8fc0c426`, `d1a007b3`)** — real Council Approval Queue panel + `terminal_pending` macro. | none stated | ENGINEERING | "a distinct governance-console feature" | entity-capability-map.md |
| event-timeline | No "on this day in your history" feature (retention capped at 30 days) | Timehop/Facebook Memories | ENGINEERING | "would require lifting the 30-day retention cap" | event-timeline-capability-map.md |
| events | **PARTIALLY CLOSED (2026-07-12):** the whole-page "out of scope" verdict was re-examined per-type, not accepted wholesale — the Events + Dashboard tabs (2 of 8) are now rewired onto the real `events.event-list`/`event-detail`/`event-create`/`event-update`/`event-delete`/`register-attendee` macros (mirroring the `calendar` lens's precedent fix), with real derived ticketing rollups (capacity/registered/revenue from live tiers/registrations) newly surfaced. The other 6 tabs (Venues/Vendors/Guests/RunOfShow/Budget/Tickets) remain generic — confirmed, per-type, as a genuinely larger IA mismatch (event-scoped nested collections vs. the tabs' flat global-list shape, plus real per-field schema gaps, plus `EventOps.tsx` already being the real, designed console for 4 of the 5) — see events-capability-map.md's "Wave 4 follow-up" section for the full per-type breakdown and disposition. | Eventbrite/Cvent/Prism.fm | ENGINEERING | "a full information-architecture rewrite... out of scope" (superseded for 2/8 tabs; still holds for the remaining 6, now with per-type evidence instead of a whole-page assumption) | events-capability-map.md |
| experience | ~~`surveyNext` branching-logic resolver has no branch-aware survey UI~~ **CLOSED (2026-07-12):** `SurveyPanel` now takes a survey one question at a time via the real `surveyNext` resolver (`branch[answer]` lookup, else fall through to the next question in array order) instead of rendering every question at once with client-only logic; added a custom survey builder with a per-answer branch editor (Typeform-style skip rules) since the existing NPS/CSAT/CES templates never populate `branch`. Also fixed a latent gap where `multi`-kind questions had no input case. 3 new tests covering multi-hop branching, terminal state, and a full branch-aware take→submit→results round trip. | Maze/UserTesting + Typeform | ENGINEERING | "build target for a future 'branching survey' UI pass" — now stale, see experience-capability-map.md's 2026-07-12 update | experience-capability-map.md |
| experience | ~~True peer endorsement needs a public-portfolio directory that doesn't exist~~ **CLOSED (2026-07-16, `83aa19fe`):** the generic cross-lens artifact layer already let any caller act on another user's published artifact via `lens.run` for a non-social domain like `experience` — confirmed by testing that infrastructure directly (a private/unpublished portfolio genuinely stays invisible across `get`/`list`/`run`) rather than assuming it. What was actually missing was narrower: nothing stopped self-endorsement, and there was no directory to discover other users' portfolios. New guard on `endorse` rejects `endorserId === artifact.ownerId`; `CareerPortfolio.tsx` gains a publish toggle, a Directory tab querying real other-user data (already scoped server-side), and peer endorsement wired to the correct portfolio id. Caught two real bugs while testing: a pre-existing test that self-endorsed via a single ctx (fixed to use two actors), and a frontend bug where a rejected endorsement was rendering as a fabricated success (only checked the outer dispatch `ok`, not the handler's own verdict). 5 new backend authz tests + 6 new frontend tests, 0 regressions (59/59 combined experience suite). | LinkedIn Skills & Endorsements | ENGINEERING | closed | experience-capability-map.md |
| export | ~~`decrypt-archive` has no UI caller (encrypt is wired)~~ **CLOSED (`7c415c8b`)** | Google Takeout/Airbyte-style export tool | ENGINEERING | "scoped future build, ~30 LOC panel" | export-capability-map.md |
| export | Obsidian-vault-style multi-file export has no frontend (needs a zip library) | Obsidian vault export | ENGINEERING | "not done to avoid an unreviewed new dependency" | export-capability-map.md |
| fashion | No retailer product-catalog integration (100M+-item DB) | Whering/Stylebook | DATA-SOURCING | "would need a licensed catalog API" | fashion-capability-map.md |
| fashion | ~~No visual drag-and-resize outfit collage canvas~~ **CLOSED (2026-07-16, `2f2ef2ed`):** outfits gain an optional per-item `layout` array (`{itemId, x, y, scale}`), following the moodboard's own per-pin x/y precedent with outfit-scaled canvas/scale-range constants. New `outfit-set-item-position` macro clamps x/y and scale independently — a resize-only or drag-only call never stomps the other axis — with a deterministic non-overlapping cascade default for unpositioned items, never persisted until the user actually touches something. Ownership enforced through the same per-user `findOutfit` scoping every outfit macro already uses. New Collage tab with real native-Pointer-Events drag (cloned from `NoteCanvasBoard`'s offset-capture technique, no new dependency) and a keyboard-operable resize handle. 23 new backend tests + 10 new frontend tests, 0 regressions (22/22 combined fashion-collage/moodboard suite). | Whering ("Dress Me") | ENGINEERING | closed | fashion-capability-map.md |
| fashion | ~~No wishlist (save desired external items) — no backend concept exists~~ **CLOSED (2026-07-12, `a7850695`)** — real `wishlist_items`-shaped per-user substrate (`STATE.fashionLens.wishlist`, same Map-per-user pattern as `items`/`outfits`/`capsules`) plus `fashion.wishlist-add`/`wishlist-list`/`wishlist-remove`/`wishlist-convert-to-item` macros (the last reuses the exact `buildWardrobeItem` constructor `item-add` uses, so a converted wishlist entry becomes a real closet item through the same code path, not a parallel one). New `FashionWishlistPanel.tsx` (structured name/price/link/note fields, no JSON-paste) mounted as a new "Wishlist" tab in `FashionClosetSection`. 8 new backend tests (`server/tests/fashion-domain-parity.test.js`) covering add/list/remove round-trip, per-user isolation, negative-price rejection, missing-name rejection, and convert-to-item (default + override fields, unknown-id failure creates nothing). 7 new frontend tests (`FashionWishlistPanel.test.tsx`) covering render, empty state, add flow, validation-rejection, remove flow, convert flow, and honest backend-error surfacing. | Whering/Stylebook | ENGINEERING | "a small, well-understood lift, deliberately not attempted" (now closed) | fashion-capability-map.md |
| fashion | ~~No moodboards (pin inspiration to a canvas)~~ **CLOSED (2026-07-16, `957e0387`)** — new `moodboard-create`/`list`/`update`/`delete`/`add-item`/`remove-item` macros, modeled on the `wishlist-*` family (a pin is a freeform external reference, not a pointer to another first-class entity the way a lookbook's `outfitIds` are). Pins carry a numeric x/y position on a bounded virtual canvas — the honest minimal version of "canvas," not a drag-and-drop engine. New `FashionMoodboardPanel.tsx` mounted as a new "Moodboards" tab. 18 new backend tests, 12 new frontend tests. | Whering/Stylebook | ENGINEERING | closed | fashion-capability-map.md |
| fashion | ~~Social feed is global, not friends-scoped; no "clone item from a friend's closet"~~ **CLOSED (2026-07-16, `0ab4494d`):** `social-feed` accepts a `friendsOnly` param filtering to confirmed friends via the real friend graph (`server/lib/friendships.js#listFriends`, the same primitive `timeline.js`'s already-shipped friends-tier privacy gate uses); no db handle fails closed to an empty feed, never falls back to the global list. New `social-clone-item` macro validates the item was part of the shared post, then calls `buildWardrobeItem` (its third caller) to create a wholly independent item in the caller's closet — never mutates the source. `FashionSocialPanel` gains a "Friends only" toggle (honest empty state) and a per-item "Clone to my closet" button. 12 new backend tests + 6 new frontend tests, 0 regressions (69/69 combined fashion suite). | Whering/Stylebook | ENGINEERING | closed | fashion-capability-map.md |
| fashion | ~~No laundry/availability status (clean/dirty/at cleaner/lent out)~~ **CLOSED (2026-07-12, `d4cb7951`)** — real `laundryStatus` field (`clean`/`dirty`/`at_cleaner`/`lent_out`) added to every closet item, defaulting to `clean` (including honest back-compat for items persisted before the field existed). Extended the existing `item-update` macro with a validated `laundryStatus` param (invalid values rejected before any field mutates, case-insensitive) rather than adding a new macro, exactly as this row's own disposition originally scoped. `item-list` gained a matching `laundryStatus` filter. `FashionClosetPanel.tsx` got a real per-item status selector (colour-coded, calls `item-update`) plus a "what can I wear" status filter row alongside the existing category filter — no separate laundry dashboard page. 7 new backend tests (`server/tests/fashion-domain-parity.test.js`) covering default-clean on add, back-compat default for a pre-existing item missing the field, valid update + persistence, all four statuses accepted, case-insensitivity, invalid-status rejection with no partial update, and `item-list` filtering. 6 new frontend tests (`FashionClosetPanel.test.tsx`) covering the selector's default render, a non-default backend value rendering correctly, changing the selector calling `item-update` with the real param and reflecting the confirmed value, honest backend-error surfacing, the status filter narrowing the grid, and "Any status" clearing the filter. | Stylebook | ENGINEERING | "Small, well-scoped future addition" (now closed) | fashion-capability-map.md |
| feed | ~~`folder-add-item` needs a folder-picker UI on the main feed's bookmark action~~ **CLOSED (`b971070d`)** | X/Threads | ENGINEERING | "a genuinely separate piece of UX design work" | feed-capability-map.md |
| fitness | ~~`gps-track` (view one past route on the map) unsurfaced~~ **CLOSED (`051e7147`)** | Strava | ENGINEERING | "~40-60 LOC, no new dependency" | fitness-capability-map.md |
| fitness | ~~`beacon-list` (browse/persist active/followed Beacons) unsurfaced~~ **CLOSED (`051e7147`)** | Strava Beacon | ENGINEERING | "~30-40 LOC" | fitness-capability-map.md |
| fitness | ~~`plan-session-move` (reschedule one specific session) unsurfaced~~ **CLOSED (`051e7147`)** | TrainingPeaks-style plan calendar | ENGINEERING | "~20-30 LOC" | fitness-capability-map.md |
| food | ~~Floor Plan & Tables / Waste Log / Prep List are unpersisted `useState` scratch pads~~ **CLOSED (2026-07-16, `148cde45`)** — new `waste-log-add`/`waste-log-list`/`waste-log-delete` (mirrors `pantry-add`/`pantry-list`/`pantry-delete`), `floorplan-table-add`/`floorplan-table-list`/`floorplan-table-update`/`floorplan-table-delete`, a deliberately-separate `floorplan-waitlist-*` family (a host-stand walk-in queue, distinct from the existing diner-facing `waitlist-join`/`waitlist-status`/`waitlist-leave` macros which require an authenticated diner + a real business record), and `prep-list-save`/`prep-list-get`/`prep-list-toggle-task` in `server/domains/food.js`. Prep List was the worse of the three: `generatePrepList` already computed a real task list, but the result was discarded into a generic `actionResult` display instead of populating the checklist — permanently empty. New `lib/hooks/use-food-ops.ts` (fetch-on-mount + optimistic-update-with-rollback) rewires all three sub-tools in `app/lenses/food/page.tsx`; `handleGeneratePrepList` now pipes the real macro's result through `savePrepTasks`, and each checklist item calls `prep-list-toggle-task`. Tests: 31 new backend (140/140 combined, 0 regressions) + 18 new frontend. | (implicit B2B kitchen-ops tooling) | ENGINEERING | closed | food-capability-map.md |
| forecast | ~~No automatic/pushed severe-event alert delivery (pull-only)~~ **CLOSED (2026-07-16)** — new `forecast-alert-sweep` heartbeat (`server/lib/world-forecast.js`, frequency 20 = ~5min on the 15s governor tick, `scope: "global"`, kill-switch `CONCORD_FORECAST_ALERT_SWEEP=0`) is exactly the extension point the capability-map doc named: it queries the real `DISTINCT user_id, world_id` pairs off `forecast_alert_subs`, calls the pre-existing `checkAlerts` per pair (unchanged — still composes a real forecast and stamps `last_fired_at` via `markSubsFired`), and on a genuine trigger emits a new `forecast:alert-triggered` event to that user's `user:<id>` socket room via the same `globalThis._concordREALTIME.io` singleton pattern `productivity-reminder-sweep` established (re-implemented locally since that helper isn't exported, not literally reused). Registered additively in `server/lib/event-shapes.js`. `AlertSubscriptions.tsx` now subscribes to the live event (clone of `ProductivityRemindersPanel`'s live-delivery UI — `useSocket`, a small ring-buffer live-fired banner, an honest connected/disconnected label, a toast), while keeping "Check against fresh forecast" as the exact same manual fallback for a tab that was closed/disconnected when the alert fired server-side. Honest scope preserved throughout: this is live delivery to a connected tab, NOT OS-level push (no service-worker Web Push pipeline exists in this codebase) — copy was updated accordingly, not just left overclaiming. `server/tests/depth/forecast-alert-sweep-behavior.test.js` (16/16) + `concord-frontend/tests/components/AlertSubscriptions-live-delivery.test.tsx` (8/8); pre-existing forecast + productivity suites re-run with 0 regressions. `server.js` was not touched — the heartbeat self-registers at module-import time. | NOAA/Windy-class dashboard | ENGINEERING | "genuine new backend surface... out of scope for a lens-UI rebuild pass" (now closed) | forecast-capability-map.md |
| foundry | ~~No player-facing runtime HUDs for the 4 Phase-7 systems in `/lenses/world`~~ **CLOSED (2026-07-16, `2b280f7b`):** four config-gated HUDs (Status Window, Size Scaling, Skill Affinity, Reincarnation prompt) mounted in the world lens, real callers of the previously-unwired `size.*`/`status.*`/`skill_affinity.*`/`reincarnation.*` macros. `useFoundrySystemGate` reads the world's real `rule_modulators.foundry.systems` (written by the Foundry compiler at publish time) — every HUD renders nothing for a world whose worldspec never selected the system, including the `skill-affinity-player` always_on system correctly gated via the same `foundry.systems` array rather than a per-system rule_modulators key that doesn't exist for it. Reincarnation trigger reuses the existing `combatState.isDead` signal; `priorState` passed honestly empty (no fabricated inherited progress) since live numeric state isn't yet plumbed to this HUD. 15 new frontend tests, 0 regressions, no backend changes. | Roblox Studio (playtest-in-world) | ENGINEERING | closed | foundry-capability-map.md |
| game | ~~No per-event XP activity log exists server-side (History tab's feed)~~ **CLOSED (2026-07-16, `e93cbafe`)** — `awardXp(s, userId, xp, gold, meta)` now logs every non-zero award to a per-user `xpLog` inside the shared function itself (so no current or future caller can bypass the log); all three real call sites (`taskComplete`/`partyContribute`/`challengeProgress`) pass real `{source,label,refId}`. New `game.xpLogList` macro. `XpActivityFeed.tsx` replaces the old honest "not tracked yet" placeholder with a real feed (server-computed lifetime totals, source filter, genuine empty state). 15 new backend tests, 13 new frontend tests. | Habitica / Steam-style achievements | ENGINEERING | closed | game-capability-map.md |
| goals | ~~Agent-goal heartbeat uses `Math.random()`-driven progress nudges~~ **CLOSED (2026-07-16, `023a7d9f`):** `processGoalHeartbeat`'s progress step now derives real deltas for the goal types with a genuine backing signal — `KNOWLEDGE_SYNTHESIS` (net-new DTU count), `PATTERN_DISCOVERY` (net-new high-authority DTU count), `CLARIFICATION` (drop in `contradictionLoad`), `CONSOLIDATION` (net-new MEGA/HYPER DTU count) — with per-goal baselines on the existing free-form `goal.meta` bag. Types with no real backing signal (`MAINTENANCE` and the rest) keep an honestly-labeled small fixed nudge rather than a fabricated precise number. Hand-verified formulas independently reproduced (e.g. 5 new DTUs at target=100 → +15 progress; contradiction load dropping 0.5→0.3 at target=100 → +20, rising → +0). 5 new backend tests, 0 regressions (97/97 combined goal-system suite). | CI/CD deployment-approval gate | ENGINEERING | closed | goals-capability-map.md |
| graph | ~~`graph.merge` operates on an ungrounded sandbox, not the real DTU graph~~ **CLOSED (2026-07-16, `b2877e49`)** — new `map-merge-nodes` macro operates on the real persisted map (`findMap`, exactly like every other mutating macro in this file), never the transient scratch object the old sandbox `graph.merge` used (left untouched). Every edge touching the losing node is re-pointed onto the survivor (never dropped), self-loops from re-pointing are removed, duplicate edges are deduplicated using `edge-add`'s own existing uniqueness rule, and the losing node's own data (notes/DTU link/central flag) is honestly folded into the survivor with merge provenance recorded, never silently discarded. New destructive-confirm "Merge Nodes" tab in `GraphParityPanel.tsx`. | none stated | ENGINEERING | closed | graph-capability-map.md |
| grounding | ~~`aggregateEvidence` has no automatic multi-source story-discovery step~~ **CLOSED (`04694adc`)** — new `discoverCoverage` macro queries GDELT for real multi-source coverage. | Ground News | DATA-SOURCING | "wire GDELT's coverage cluster... genuinely global, not US-centric" | grounding-capability-map.md |
| healthcare | ~~`protocolMatch` has no protocol library to match against~~ **CLOSED (`fd40bae6`)** — 16 real, cited clinical protocols authored (`content/healthcare-protocols.json`). | Epic/Cerner-style CDS | CURATION | "needs either a real curated protocol library... or a real external guideline-API integration" | healthcare-capability-map.md |
| healthcare | ~~`vision` (photo→LLaVA analysis) has no image-upload UI anywhere~~ **CLOSED (2026-07-16)** — new `photo-notes-list`/`photo-notes-add`/`photo-notes-delete` macros (`server/domains/healthcare.js`, cloned from the file's own `allergies-*` CRUD family / `STATE.healthLens.photoNotes` bucket) turn a photo into a durable per-patient chart entry: `photo-notes-add` calls the SAME real `callVision`/`callVisionUrl` + `visionPromptForDomain("healthcare")` the standalone `vision` macro uses (never re-derived or mocked) and only persists a record when that call genuinely succeeds — a down/unavailable vision brain surfaces as `{ok:false,error:"vision analysis failed"}` with nothing written to the chart, never a fabricated "analyzed" success. The plain `vision` macro is untouched (still a real one-off analysis path for existing callers). New "Photos" tab on `PatientChartPanel.tsx`: an image-upload control (FileReader.readAsDataURL → base64 data: URL, the same idiom already used by `TravelDocsPanel.tsx`/`PjTaskDetail.tsx` — no new upload component needed), the real returned `analysisResult` rendered per photo, delete, and an honest error banner on a failed vision call. 8 new backend behavioral tests (`server/tests/depth/healthcare-photo-notes-behavior.test.js` — validation rejections, honest vision-unavailable failure against the real `callVision()` codepath with no Ollama reachable in this sandbox, and fixture-seeded list/delete/per-patient-scoping) + 5 new frontend tests (`concord-frontend/tests/components/PatientChartPanel-photos.test.tsx`). 166 pre-existing healthcare backend tests + 0 regressions. | Epic/Cerner-style CDS | ENGINEERING | closed | healthcare-capability-map.md |
| history | ~~No notable-person/biography tracking as a first-class analyzed entity~~ **CLOSED (2026-07-16, `bc0ba5e6`)** — new `figure-add`/`figure-list`/`figure-update`/`figure-delete`/`figure-link-event`/`figure-unlink-event` macros in `server/domains/history.js`, modeled on the file's own `timeline-create`/`event-add`/`era-add` precedent (`STATE.historyLens`, shared `hsClean`/`hsId`/`hsNow`/`hsActor`/`hsYear` helpers). The real differentiator per this row's own disposition — "timeline-linkage" — is genuine: `figure-link-event` validates `timelineId`/`eventId` against the caller's actual timeline data (rejects a fabricated id) and `figure-list` re-derives every linked event LIVE on every read, so a since-deleted timeline/event honestly surfaces as `found:false` rather than vanishing or staying falsely "valid". `figure-update` is a real partial update. `FiguresNotebook.tsx` rewired off the generic disconnected `useLensData` notebook store onto these macros, with a real event-linkage UI (linked list with honest not-found state, unlink, and a link picker sourced from real `timeline-list`/`timeline-detail` calls) and an accurate disclosure banner replacing the old "not backend-validated" framing. Tests: 19 new backend + 9 new frontend, 84/84 combined backend suite (0 regressions). | TimelineJS/Wikipedia | ENGINEERING | closed | history-capability-map.md |
| hr | No ACH/direct-deposit payroll disbursement (`payroll-run` computes but never moves real money) | BambooHR/Rippling | ENGINEERING | "explicitly out of scope for a compute-and-record HRIS layer" (money-transmission policy) | hr-capability-map.md |
| hr | ~~No I-9/E-Verify employment-eligibility compliance tracking~~ **CLOSED (2026-07-12, `99238750`)** — 7 new `hr.i9-*` macros (`i9-add/list/verify/reject/everify-submit/document-attach/status`) following the `compliance-doc-*` shape, plus a real I-9/E-Verify sub-section in `HrCompliancePanel`. See `hr-capability-map.md` for the full writeup. | BambooHR/Rippling | ENGINEERING | "~150-LOC addition" | hr-capability-map.md |
| hr | No external job-board syndication (postings internal-only) | BambooHR/Rippling | DATA-SOURCING | "following the Gmail/Calendar connector pattern" | hr-capability-map.md |
| hypothesis | ~~`analysisHistory` has no natural single-click surface~~ **CLOSED (`543adb1f`)** — History tab on StatsWorkbench. | none stated | ENGINEERING | "a future analytics/reports tab could adopt it, out of scope this wave" | hypothesis-capability-map.md |
| ingest | No live connector execution (`runSync` needs caller-supplied records) | Airbyte/Fivetran | ENGINEERING + DATA-SOURCING | "each concrete source needs its own real credential/egress path" | ingest-capability-map.md |
| ingest | No heartbeat drains due schedules into runs | Airbyte/Fivetran | ENGINEERING | "once live connector execution exists" | ingest-capability-map.md |
| ingest | ~~No schema auto-inference/column-type detection on preview~~ **CLOSED (2026-07-16, `e4f73b1d`)** — new `detectSchema` macro promotes `validateSchema`'s field-name-only no-schema branch into real per-field type inference (type/nullable %/uniqueness %/sample values), sampled across every record. A field is honestly reported as `"mixed"` (with a breakdown) rather than forced to a majority type when the sample genuinely disagrees. New "Detect Schema" button + real per-field table in the ingest page. 9 new backend tests, 4 new frontend tests. | Airbyte | ENGINEERING | closed | ingest-capability-map.md |
| dreams | "Featured"/highlight ranking across entries has no significance signal in the backend | Apple Photos "Memories" | ENGINEERING | "deliberately not built to avoid inventing a fabricated signal" | dreams-capability-map.md |
| inheritance | No real-world probate/estate reference data (statutory intestacy shares, per-state timelines) | Trust & Will / FreeWill | DATA-SOURCING | "ingest a real open source... rather than fabricate share tables" | inheritance-capability-map.md |
| insurance | ~~No persisted client/CRM contact record~~ **CLOSED (2026-07-12, `ddbd111f`)** — `server/domains/insurance.js` gained a `client-add`/`client-list` macro pair plus a `resolveClientRef` helper (STATE-backed `insLens.clients` Map, mirroring plumbing's `clientAdd`/`clientList`/`resolveClientRef` and landscaping's `client-add`/`client-list` shape under this file's own hyphenated naming convention), with an optional `clientId` wired additively into `policy-add`/`claim-file`/`certificate-issue` (free-text `insuredName`/`certificateHolder` behavior preserved byte-for-byte when `clientId` is omitted) plus a real combobox (`ClientAutocomplete.tsx`, restyled to this lens's blue palette) wired into `InsurancePoliciesPanel`/`InsuranceClaimsPanel`, and a new "Clients" tab (`InsuranceClientsPanel.tsx`) in `InsuranceWalletSection` showing each client's aggregated policy/claim book. See `insurance-capability-map.md` for the full detail + test list. | Applied Epic / EZLynx | ENGINEERING | "real, buildable gap — now closed" | insurance-capability-map.md |
| insurance | ~~No producer compliance tracking (CE credits, license renewal, E&O status)~~ **CLOSED (2026-07-16, `1383984a`)** — new `producer-compliance-add`/`producer-compliance-list`/`producer-compliance-update`/`producer-compliance-remove` macros in `server/domains/insurance.js`, attached to the existing agent/broker roster (`agent-add`/`agent-list` — no separate "producer" entity invented) — the 4th instance this session of the read-time-derived expiry pattern first built for plumbing/masonry/landscaping certs. Four genuinely distinct categories rather than one generic shape: `ce_credits` (progress toward a requirement, with a divide-by-zero-guarded percent), `license_renewal`, `eo_insurance`, `carrier_appointment`. `agentId` is validated against the real roster; `category` is hard-rejected on an invalid value (not silently defaulted, since a mis-tracked compliance category is a real licensing-risk concern). Status (overdue/due_soon/scheduled) and `agentName`/`agentFound` are re-derived live on every list call. New `ProducerCompliance.tsx` panel — an 8th `AmsWorkbench` tab — with a genuinely category-adaptive form, a real agent picker, status badges, a CE-credit progress bar, and prominent overdue/due-soon risk-summary tiles. This closes insurance's entire ENGINEERING-class deferred list — only the paid-API carrier-quote-comparison row remains, correctly DATA-SOURCING. Tests: 40 new backend (177/177 combined, 0 regressions) + 16 new frontend. | Applied Epic / EZLynx | ENGINEERING | closed | insurance-capability-map.md |
| insurance | Real carrier quote comparison needs a paid, per-state-licensed broker API | Applied Epic / EZLynx | DATA-SOURCING (paid) | "intentional honest-failure documented in code, not silently faked" | insurance-capability-map.md |
| integrations | Real OAuth connect flow for catalog connectors (only Gmail + Calendar are real today) | Zapier | ENGINEERING + DATA-SOURCING | "next thin adds on the connector-agnostic connectorFetch core" | integrations-capability-map.md |
| integrations | ~~`dueSchedules` (N-due-now indicator) not surfaced~~ **CLOSED (`543adb1f`)** — real "N due now" badge on WorkflowsPanel. | Zapier | ENGINEERING | "Low value; deferred" | integrations-capability-map.md |
| integrations | ~~`verifyWebhookSignature` widget not surfaced~~ **CLOSED (2026-07-12, `543adb1f`)** — `WebhookSignatureVerifier.tsx` built and mounted live at `app/lenses/integrations/page.tsx:402`. Row was left unstruck in the ledger despite the work landing; corrected here. | Zapier | ENGINEERING | closed | integrations-capability-map.md |
| kingdoms | ~~`revokeDecree` has no ruler-authorization check~~ — **CLOSED (STALE, already fixed)** (commit `fd2f0f9e`, 2026-07-11, independent of this ledger's own execution batches): `revokeDecree` now checks the caller is the realm's current player ruler, mirroring `proposeDecree`'s pattern; regression-tested (5/5) | none stated | ENGINEERING | closed | kingdoms-capability-map.md |
| lab | ~~No barcode/2D-barcode label printing for samples/reagents~~ **CLOSED (2026-07-16, `595f96c9`)** — new `lab.label-generate`/`label-list` macros produce a deterministic scanner-parseable payload string plus structured metadata; the file has no separate "sample" collection, so `recordType:"sample"` maps onto the existing `constructs` store (documented inline). `LabelPrintModal` in `ELNWorkbench.tsx` renders a real, spec-compliant QR code via the pre-existing `qrcode` dependency (already used identically in `QRCodeReceive.tsx`) — a real scanner decodes it back to the payload. Print uses the browser's native print dialog; no physical-printer integration, no fake "sent to printer" state. 8 new backend tests, 5 new frontend tests (asserting the QR library is actually invoked with the exact payload). | Benchling / LabWare LIMS | ENGINEERING | closed | lab-capability-map.md |
| lab | No direct live instrument integration (CSV paste/upload only) | Benchling / LabWare LIMS | ENGINEERING | "a hardware-integration project, not a UI gap" | lab-capability-map.md |
| lab | No multi-user lab roles/permissions (PI/tech/guest tiers) | Benchling / LabWare LIMS | ENGINEERING | "a new permissions layer, not a lens-page fix" | lab-capability-map.md |
| landscaping | ~~No job scheduling/dispatch macro~~ **CLOSED (2026-07-12, `fee9def4`)** — `server/domains/landscaping.js` gained a `job-schedule`/`job-list`/`job-complete` triple (per-user `globalThis._concordSTATE`-backed Map, mirroring plumbing's `dispatchAssign`/`dispatchBoard`/`jobComplete` shape under this file's hyphenated naming convention) plus a real crew-lane dispatch board UI (`JobDispatchBoard.tsx`, new 5th "Jobs" tab in `app/lenses/landscaping/page.tsx`). See `landscaping-capability-map.md` for the full detail + test list. | none stated | ENGINEERING | "real, buildable gap" | landscaping-capability-map.md |
| landscaping | No permit/setback code-reference library | none stated | CURATION | "fabricating code text would be a honesty violation; deferred" | landscaping-capability-map.md |
| landscaping | No browsable supplier materials catalog | none stated | CURATION | "needs sourced supplier/spec data this domain doesn't have" | landscaping-capability-map.md |
| landscaping | ~~No persisted Client/CRM entity (free-text field only)~~ **CLOSED (2026-07-12, `5eae9fda`)** — `server/domains/landscaping.js` gained a `client-add`/`client-list` macro pair plus a shared `lsResolveClientRef` helper (per-user `globalThis._concordSTATE`-backed Map, mirroring plumbing's `clientAdd`/`clientList`/`resolveClientRef` shape under this file's hyphenated naming convention), with an optional `clientId` wired additively into `proposal-build`/`invoice-from-proposal`/`job-schedule` (free-text `client` behavior preserved byte-for-byte when `clientId` is omitted) plus a real combobox (`ClientAutocomplete.tsx`, restyled to this lens's emerald palette) wired into `ProposalBuilder`. See `landscaping-capability-map.md` for the full detail + test list. | none stated | ENGINEERING | "real, buildable gap — now closed" | landscaping-capability-map.md |
| landscaping | ~~No proposal→invoice conversion / payment-tracking status machine~~ **CLOSED (2026-07-12, `d2e775f9`)** — `server/domains/landscaping.js` gained a real `draft`→`sent`→`accepted`→`paid` invoice status machine (`invoice-from-proposal`/`invoice-list`/`invoice-send`/`invoice-accept`/`invoice-record-payment`, same per-user STATE-backed pattern as the Jobs triple, math re-derived server-side from `computeProposal` rather than trusting client-supplied totals) plus a designed "Invoices" tab (`InvoiceTracker` in `GardenStudio.tsx`) with a "Convert to invoice" action on the Proposal tab. See `landscaping-capability-map.md` for the full detail + test list. | none stated | ENGINEERING | "real, buildable gap — now closed" | landscaping-capability-map.md |
| landscaping | ~~No inspection/walkthrough record model~~ **CLOSED (2026-07-16, `a3cc5c63`)** — new `inspection-add`/`inspection-list`/`inspection-update` macros modeled directly on masonry's shipped precedent (`3e1234af`): `jobId` required (references a real `job-schedule` record, not free text), `jobTitle`/`jobFound` re-derived live so a renamed/deleted job displays honestly, real landscaping-trade categories (final walkthrough, irrigation system check, plant health/establishment, hardscape/drainage, seasonal maintenance audit, warranty/punch-list review, erosion/sediment control, pre-installation site review), pass/fail/pending with required deficiency notes + re-inspection date on fail. New "Inspections" tab in `GardenStudio.tsx`. Tests: 148/148 across all 4 landscaping backend test files combined. | none stated | ENGINEERING | closed | landscaping-capability-map.md |
| landscaping | ~~No crew certification record~~ **CLOSED (2026-07-16, `a3cc5c63`)** — new `cert-add`/`cert-list`/`cert-remove` macros deliberately mixing two proven precedents: expiry semantics (read-time-derived, no sweep) follow plumbing's shape since nothing in this domain gates a downstream action on cert status; record shape (flat per-user list keyed by free-text `crewMemberName`) follows masonry's shape since landscaping has no persisted crew/tech entity to attach a cert to. Real landscaping-trade categories (NALP Certified Landscape Professional, COLP, pesticide/herbicide applicator license, ISA Certified Arborist, Irrigation Association CIT/CID, backflow prevention, OSHA 10, equipment operator cert, first aid/CPR). New "Certifications" tab. Tests: 39/39 combined landscaping frontend regression. | none stated | ENGINEERING | closed | landscaping-capability-map.md |
| law | ~~Case-law search has no semantic/natural-language query mode~~ **CLOSED (2026-07-12, `a18c2be0`)** — `law.courtlistener-search` forwards CourtListener's real v4 `semantic=true` param (source: `freelawproject/courtlistener` GitHub — `cl/search/forms.py`, `cl/search/api_views.py`, `cl/search/api_serializers.py`; courtlistener.com/free.law themselves were network-blocked in this environment), additive/byte-identical when unrequested; `LegalCaseSearch.tsx` got a real Keyword/Semantic toggle + per-result match-score badge. | CourtListener | ENGINEERING | "add a semantic param + UI toggle" | law-capability-map.md |
| law | ~~No citation graph ("who cites this opinion")~~ **CLOSED (2026-07-16, `619df534`):** new `law.citation-graph` macro hits CourtListener's real `opinions-cited` viewset, cloned from the existing `courtlistener-search`/`recap-docket-search` fetch/error pattern. Network to courtlistener.com is policy-blocked in this sandbox, so the field shape (citing/cited opinions as resource URLs, no case name at this endpoint) was verified against the Free Law Project's own open-source Django repo rather than guessed — matching the discipline `recapDocketSearchHandler`'s own header comment already documents for this same constraint; the macro never fabricates a case name the API doesn't actually provide. `LegalCaseSearch.tsx` (shared by both the `law` and `legal` lens pages) gains an expandable "Citing opinions" section per result with honest loading/error/empty/populated states. 11 new backend tests + 4 new frontend tests, 0 regressions (177/177 combined law suite). | CourtListener | ENGINEERING | closed | law-capability-map.md |
| law | ~~No RECAP/PACER docket search (opinions only)~~ **CLOSED (`2ac5106d`)** — real RECAP Archive search via CourtListener's `type=r`, honest per-document tri-state (free/paywalled/unknown). | CourtListener | ENGINEERING | "a real, separately-scoped macro + UI" | law-capability-map.md |
| law | ~~No search/docket/citation alerts (no persistence/notification substrate)~~ **CLOSED (2026-07-16, `e0cc274a`)** — new `search-alert-add`/`list`/`remove`/`check` macros. `search-alert-check` calls the exact same `courtlistener-search`/`recap-docket-search` handler functions in-process (refactored from anonymous to named consts for this), diffing fresh results against the prior check's `lastSeenResultIds`. A failed search never touches that baseline — never fabricates "0 new results" on a genuine failure. No heartbeat wired (this lens has no notification channel, so a silent background sweep would consume the "new" flag before the user saw it); left honestly manual "Check now" only, labeled as such in the UI. No fabricated "citation" alert type — CourtListener citation lookup already IS case-law search with a citation string as the query. | CourtListener | ENGINEERING | closed | law-capability-map.md |
| law | ~~Patent search accepts one field at a time, no boolean multi-field query builder~~ **CLOSED (2026-07-16, `4916a1cb`):** `uspto-patent-search` accepts an optional `params.filters: [{field, value}]` + `params.combinator: "and"\|"or"`, building a real PatentsView `_and`/`_or` clause of the same `_text_phrase` clauses the single-field path already builds (factored into a shared `_usptoFieldClause` helper). Invalid filter rows are dropped; zero valid filters falls back byte-identically to the original single-`query`/`field` behavior — confirmed via a dedicated pinning test. `PatentSearch.tsx` gains an opt-in "Advanced" mode with a repeatable filter-row builder + AND/OR toggle; the default single-field quick search is unchanged. 5 new backend tests (byte-identical fallback, AND, OR, invalid-row dropping, empty-filters honesty) + 9 new frontend tests. | USPTO Patent Public Search | ENGINEERING | closed | law-capability-map.md |
| law | Patent search lacks full claims text / prosecution history fields | USPTO Patent Public Search | DATA-SOURCING | "not in the current field set" | law-capability-map.md |
| law | Patent search lacks legal status (active/expired/litigated) | USPTO Patent Public Search | DATA-SOURCING | "not in the current field set" | law-capability-map.md |
| law | ~~Contract dashboard lacks deeper trend analytics~~ **CLOSED (2026-07-16, `bcf157bc`):** new `contract-trends` macro, kept separate from the cheap point-in-time `contract-dashboard` since the trend walk is naturally heavier. Computes cycle-time-to-signature, spend-by-counterparty-by-month, and renewal-rate-by-month from real `createdAt`/`signatures[].signedAt`/`value`/`counterparty`/`renewal`-kind-obligation fields — nothing fabricated or estimated. Every bucket carries `hasData` and, where relevant, `hasTrend` (≥2 distinct periods), so thin history renders an honest "not enough data" rather than a misleading single-point line. `LawContracts.tsx` renders the series via the already-shared `ChartKit`. 4 new backend tests with a hand-computed fixture (verified independently) + 4 new frontend tests, 0 regressions (103/103 combined law suite). | Ironclad | ENGINEERING | closed | law-capability-map.md |
| law | ~~`law.draft`/`law.cite` exist but need id-scoped UI wiring~~ **CLOSED (`38bfe84c`)** | Ironclad | ENGINEERING | "no backend work needed" | law-capability-map.md |
| law | No real-time multi-party collaborative redlining | Ironclad | ENGINEERING | "large scope — flagged future" | law-capability-map.md |
| law-enforcement | ~~No persisted "Case" record type server-side~~ **CLOSED (2026-07-12, `a68778e4`)** — migration 362 `le_cases` + `caseCreate`/`caseGet`/`caseList`/`caseUpdate`/`caseLinked` macros + `CaseManagementPanel.tsx` Cases tab | none stated | ENGINEERING | "a new macro group... out of scope for this pass" | law-enforcement-capability-map.md |
| legal | No cross-jurisdiction, state-specific court procedural rules (FRCP-only) | Clio (implied) | DATA-SOURCING | "no honest free source found... an honest scope limit" | legal-capability-map.md |
| literary | ~~`crystallize` (resonance-salience passage ranking) has no UI~~ **CLOSED (`bc1e820f`)** | none stated | ENGINEERING | "real, cheap reads with no external dependency" | literary-capability-map.md |
| lock | Sovereignty "invariants" are frozen-constant, not a live runtime-checked pass/fail history | none stated | ENGINEERING | "not fixed in this pass" | lock-capability-map.md |
| logistics | `inventoryAudit` has no real inventory data source (WMS/Shopify feed) | none stated | DATA-SOURCING | "needs a real warehouse feed integration" | logistics-capability-map.md |
| mail | ~~No way to browse/search other users to pick a mail recipient (raw userId text only)~~ **CLOSED (`a1d21fdc`)** — real recipient search UI, reusing an existing endpoint. | none stated | ENGINEERING | "recorded here as a deferred enhancement" | mail-capability-map.md |
| marketing | No dedicated marketing-sending domain/deliverability infrastructure | HubSpot Marketing Hub / Mailchimp | ENGINEERING | "No UI claims a dedicated sending domain exists" | marketing-capability-map.md |
| marketing | No external ad-platform integration (spend manually entered) | HubSpot Marketing Hub / Mailchimp | DATA-SOURCING | "Deferred, not faked" | marketing-capability-map.md |
| marketplace | ~~No public cross-seller browse for the e-commerce store (listings only visible in own shop)~~ **CLOSED (2026-07-12, `78337966`) — was already shipped in `f3c06f15`, entry was stale.** The `storefront-browse` macro (`server/domains/marketplace.js:721`) + `StorefrontPanel.tsx` buyer catalog UI already existed, wired via `ShopfrontSection`'s always-visible "Storefront" nav item. This pass verified the contract and added reciprocal-visibility + no-leak + kind-filter test coverage (backend + frontend) rather than re-implementing. | Etsy-shape | ENGINEERING | "was already real; this pass added verification + tests, not new code" | marketplace-capability-map.md |
| marketplace | "Plugin"-type Browse items have no real purchase path | Etsy-shape | ENGINEERING | "Left as an honest per-item checkout error" | marketplace-capability-map.md |
| marketplace | ~~Buyer "Download" button calls a nonexistent fulfillment route~~ **CLOSED (`7ef97159`)** — fixed the one real fulfillment path (plugins: was GET, needed POST); every other purchased-item type now shows an honest "no automated delivery yet" instead of a fabricated success. | Etsy-shape | ENGINEERING | "left as a known, honestly-failing gap" | marketplace-capability-map.md |
| masonry | ~~No Inspections or Certifications tabs~~ **CLOSED (2026-07-12, `3e1234af`)** — new `inspection-add`/`inspection-list`/`inspection-update` macros modeled on `change-order-*` per this doc's own prior recommendation: an inspection MUST reference a real `schedule-list` job (`jobId` required, not soft-defaulted like change-orders), 8 real inspection-type categories, pass/fail/pending with required deficiency notes + re-inspection date on failure, `jobTitle`/`jobFound` re-derived live on every list call (honest if the job is later renamed/deleted). New `cert-add`/`cert-list`/`cert-remove` for crew certifications (13 real masonry-trade categories — OSHA 10/30, lift-operator tiers, confined space, silica exposure, NCMA/MCAA mason tiers, etc.) using plumbing's read-time-derived `expiryStatus`/`isExpired` shape (not hr's I-9 sweep-and-persist shape — verified nothing in masonry gates a downstream action on cert status today, so the lighter pattern applies). Two new tabs in `ContractorSuite.tsx`, both real designed forms (job picker sourced from real jobs, category selects, expiry badges), no generic scaffold. Tests: 67 new backend (23+20+24 across the three masonry test files, individually + combined re-run, 0 fail) + 14 new frontend, all independently re-run. | JobNimbus / Buildertrend | ENGINEERING | closed | masonry-capability-map.md |
| materials | No periodic-table/element browser UI | MatWeb / Ansys Granta MI | DATA-SOURCING | "would need a new data source" | materials-capability-map.md |
| materials | ~~No failure-analysis/fractography workflow~~ **CLOSED (2026-07-16, `0c687349`)** — new `fractographyAnalysis`/`fractographyRootCause` macros, evidence-scored against five real ASM Handbook Vol.11 failure modes (ductile/brittle/fatigue/SCC/creep — the last two using a real homologous-temperature calculation and a curated susceptible-material×agent table). Ambiguous evidence is honestly reported as "mixed evidence" with both candidates rather than forced to a single confident verdict; missing material identity or missing any surface observation is honestly rejected. New `FractographyPanel.tsx` with a real discrete-observation intake form (not free text), mounted as a new tab in `MaterialsToolkit.tsx`. 17 new backend tests, 5 new frontend tests. | MatWeb / Ansys Granta MI | ENGINEERING | closed | materials-capability-map.md |
| materials | No PLM/CAD integration | MatWeb / Ansys Granta MI | ENGINEERING | "Deferred as a connector-tier project" | materials-capability-map.md |
| math | ~~No general polynomial root-finder (deg>4) or fuller symbolic-integration table~~ **CLOSED (2026-07-16, `fde31825`, root-finder only)** — new `polynomialRootsGeneral` macro implements Durand-Kerner (Weierstrass) simultaneous iteration in complex arithmetic, finding all n roots of a degree-n polynomial (up to degree 60) at once, real or complex, without a derivative or bracketing interval — the arbitrary-degree companion to `polynomialAnalysis`'s degree<=4-only closed-form/Newton-Raphson roots. Every claimed root value was hand-verified against known cases (including a genuine complex-root case, x⁴-1 → {1,-1,i,-i}) before finalizing. Each root reports its own `converged` flag (step-size AND residual both below tolerance); a triple root genuinely fails to converge within the iteration budget and is honestly reported `converged:false` rather than presented as exact. New "Poly⁺" action in `MathActionPanel.tsx` renders complex roots and surfaces non-convergence honestly. Symbolic-integration table remains open. 15 new backend tests, 7 new frontend tests. | Wolfram Alpha / Desmos / Mathematica | ENGINEERING | closed (root-finder); symbolic integration still open | math-capability-map.md |
| math | Formulas tab uses hand-rolled Unicode LaTeX approximation, not real KaTeX/MathJax | Wolfram Alpha / Desmos / Mathematica | ENGINEERING | "cosmetic-only gap" | math-capability-map.md |
| mentorship | ~~No group-session support (strictly 1:1)~~ **CLOSED (2026-07-16, `b148704e`)** — `groupSessions` is a `Map<sessionId, session>` (distinct from the file's usual `Map<userId, Array>` shape, since a group session must be discoverable by both host and any of N joining/leaving attendees). New macros `group-session-create`/`group-session-list`/`group-session-join`/`group-session-leave`/`group-session-update` in `server/domains/mentorship.js`; `GroupSessionsPanel.tsx` surfaces a Session-ID share/join flow (discovery scoped to sessions the caller is already involved with, matching the 1:1 flow's own privacy posture — no fabricated "browse all public sessions" feature). 33 new backend tests, 16 new frontend tests. | ADPList / MentorcliQ | ENGINEERING | closed | mentorship-capability-map.md |
| mentorship | ~~`review-list` star-histogram has no dedicated UI~~ **CLOSED (`24788506`)** | ADPList / MentorcliQ | ENGINEERING | "one more panel section, no backend work" | mentorship-capability-map.md |
| mesh | ~~No UI lets a user send a DTU over the mesh (the lens's namesake capability)~~ **CLOSED (`e90b7181`)** — also fixed a related DTU-leak gap in the same commit. | Meshtastic / Briar | ENGINEERING | "Flagging for a future Wave 4 gap-closure pass" | mesh-capability-map.md |
| message | External Slack/Sheets/GitHub/Notion OAuth connectors gated on operator-supplied client secrets | none stated | DATA-SOURCING-adjacent | "Out of scope for this pass; not a defect in the `message` lens" | message-capability-map.md |
| meta | ~~`POST /api/inventory/refresh` has no frontend caller~~ **CLOSED (`ba643e66`)** | Backstage / Sourcegraph | ENGINEERING | "noted here rather than silently dropped" | meta-capability-map.md |
| metacognition | ~~`biasDetection` has no real data source (journal schema lacks per-option score/evidence fields)~~ **CLOSED (2026-07-16, `e39aad6a`)** — `journalLog` now accepts a richer per-option `{name, score, evidence}` shape (legacy flat strings still work) plus optional `chosen`/`initialAnchor`/`investedCost` fields, with honest rejection of malformed rich input. New `journalBiasDetection` adapter macro sources real decisions from the caller's own journal and runs the same math as `biasDetection` (extracted into a shared `computeBiasReport()`). Proven the real math actually detects real bias: a deliberately-engineered two-decision journal produces `biasesDetected: 3` (anchoring/confirmation/sunk-cost, all severity "high", `biasIndex: 1`) computed by the macro, not hand-asserted. New collapsible "Advanced" entry form + a real "Bias Detection" panel in `DecisionJournal.tsx`. 14 new backend tests, 9 new frontend tests. | none stated | ENGINEERING | closed | metacognition-capability-map.md |
| metacognition | ~~`select_strategy`/`adjust_confidence` have no designed UI entry point~~ **CLOSED (2026-07-12, `f2800b32`)** — both macros were already real (verified against `server/server.js` source + the full `/api/lens/run` unwrap path before writing any code); the gap was purely the missing UI, and the doc's own bar ("needs real UI thought, not a bolted-on generic button") is what got built: a new `ReasoningToolkit` panel with two bespoke sub-cards — Strategy Advisor (labeled problem textarea → suggested strategy badge + description + alternative chips) and Confidence Adjuster (domain input + confidence slider → original-vs-adjusted percentages with a green/red/gray directional indicator + the macro's real explanation text). Honest loading/error states on both flows; nothing fabricated on failure. Tests: 11/11 new (`ReasoningToolkit.test.tsx`), re-run independently. | none stated | ENGINEERING | closed | metacognition-capability-map.md |
| mining | ~~No environmental compliance/reclamation tracking~~ **CLOSED (2026-07-16, `df03ecea`)** — new `compliance-log`/`compliance-list`/`compliance-update`/`reclamation-update`/`reclamation-list` macros in `server/domains/mining.js`, mirroring the existing `incident-log` foreign-key-into-a-site pattern. Compliance category/status are hard-rejected on an unrecognized value (a mis-tracked regulatory category is a real honesty concern, unlike this file's other tolerant enum fallbacks); `isOverdue`/`daysUntilExpiry` are re-derived live, never stored. Reclamation is a single ongoing per-site record (phase, acres disturbed/reclaimed, bond amount/status) backing the `"reclamation"` status value `site-update` already accepted but had nothing behind it; `acresReclaimed` clamps to `acresDisturbed` rather than rejecting. New `EnvironmentalCompliance.tsx` panel (site picker, risk tiles, reclaimed-acres progress bar). This closes mining's actionable ENGINEERING gap — CAD-grade pit optimization remains explicitly out of scope. Tests: 39 new backend (94/94 combined, 0 regressions) + 14 new frontend. | Deswik / Micromine (+ MSHA) | ENGINEERING | closed | mining-capability-map.md |
| mining | No CAD-grade constraint-driven pit optimization (geotech limits) | Deswik / Micromine | ENGINEERING | "a substantial new backend engine, out of scope here" | mining-capability-map.md |
| ml | No actual GPU-backed model training | Weights & Biases / Hugging Face Hub | ENGINEERING | "well beyond a UI-parity pass" | ml-capability-map.md |
| news | ~~Personalized reader (follows/topics/saves/history/alerts/digest/offline — ~34 macros) only reachable via generic strips~~ **CLOSED (`9bc82008`)** — the "generic strips" framing was itself stale (the components were fully built, just unmounted, not generic); mounted as a real "My Reader" desk + closed the 5 macros with no component at all. | none stated | ENGINEERING | "ranked next-wave surfaces, not fabricated features hidden behind dead buttons" | news-capability-map.md |
| offline | ~~No filtered/scoped replication (per-collection/per-query sync)~~ **CLOSED (2026-07-16, `ade4e551`)** — new `filterCreate`/`filterList`/`filterDelete` macros, CouchDB `_filter`-style saved predicates. A filter matches on the document id's collection-prefix convention (`note:trip-plan` → collection `"note"`, the one real always-present convention this schemaless `{id, body}` doc store has) and/or real field conditions (eq/contains/gt/lt) against the stored body — nothing invented. `replicationPull` gains an optional `filterId`; an unknown/foreign id is a hard error, never a silent fall-through to unfiltered. Sequence numbers stay absolute across filtered and unfiltered streams (filtering happens after the `seq > since` cut), and the frontend gives each filter its own sync checkpoint so switching filters can't corrupt either stream's `since` — pinned by a dedicated incremental-sync-consistency test. New "Scoped replication filters" panel in `ReplicationPanel.tsx`. 17 new backend tests, 12 new frontend tests. | PouchDB/CouchDB, Workbox | ENGINEERING | closed | offline-capability-map.md |
| offline | ~~No multi-device conflict provenance~~ **CLOSED (2026-07-16, `fdfb34e6`):** `replicationPush` accepts an optional per-push `deviceId`, stamped onto stored docs, changes-feed entries, and both sides of any conflict record; `replicationPull` surfaces it on each mapped change; `mergeResolve` records the resolving device. Omitting `deviceId` is fully backward compatible (writer field is simply null). Kept as a distinct field from `syncCheckpoint`'s `replicationId` (a per-sync-stream identity, not per-device) to avoid conflating the two and regressing the already-closed filtered-replication checkpoint isolation. `local-store.ts` gains `getDeviceId()` (stable per-browser id via `crypto.randomUUID()`, persisted in `localStorage`, honestly `null` when unavailable); `ConflictMergePanel` renders "written by device X" (or an honest "unknown device") on both sides of a conflict. 10 new backend tests + 21 new frontend tests, plus the pre-existing filtered-replication suite reconfirmed unregressed (17/17). | PouchDB/CouchDB, Workbox | ENGINEERING | closed | offline-capability-map.md |
| ops-telemetry | ~~`/api/admin/liveness` uses `requireAuth()` only, inconsistent with the page's uniform gate~~ **CLOSED (2026-07-16, `67ed6e90`):** a real auth-tier gap — any logged-in user, not just owner/admin/sovereign/founder, could read this operator metric. Fixed to `requireRole("owner", "admin", "sovereign", "founder")`, matching every sibling `/api/admin/*` route on the same page exactly. Required a one-line companion fix at the route's registration call site in `server.js` (`requireRole` was a declared-but-never-passed parameter — calling it would have thrown synchronously and silently dropped every route registered after it in that file). New E2E test independently reproduced: reverting only the route's middleware turns 2/4 assertions red, with the concrete failure being `200` where `403` is now correctly returned for a non-privileged caller. | Datadog / Grafana | ENGINEERING | closed | ops-telemetry-capability-map.md |
| ops-telemetry | Inference-cost metering covers only 3 of ~36 LLM call sites | Datadog / Grafana | ENGINEERING | "a cross-cutting change to ~30+ files" | ops-telemetry-capability-map.md |
| ops-telemetry | ~~Two independent unjittered 5s polling timers on one page~~ **CLOSED (`eb9c9040`)** | Datadog / Grafana | ENGINEERING | "tracked separately as a Wave 4 item" | ops-telemetry-capability-map.md |
| organ | ~~Roster schema lacks role/demographics fields for team-composition analysis~~ **CLOSED (2026-07-12, `a49fedf2`)** — `normEmployee` (`server/domains/organ.js`) now normalizes an optional `role` (validated against the exact 9-entry Belbin set `teamComposition` already read) and an optional, capped/cleaned `demographics` bag, both omitted (not empty) when unset so old rosters round-trip unchanged; `OrgDesigner`'s `EmployeeModal` gained a Belbin-role select + Gender/Age-band demographics selects (closed dropdowns, not free text); `OrgAnalysisPanel` (`app/lenses/organ/page.tsx`) now forwards both fields into the `teamComposition` macro call and renders real Belbin-balance/Simpson's-diversity results (or an honest "not offered" line) instead of silently dropping them. 6 new backend behavioral tests (`server/tests/depth/organ-behavior.test.js`, 33/33 total) + 3 new frontend tests (`OrgDesigner.employeeModal.test.tsx`). See `organ-capability-map.md`'s "Team `role` (Belbin) / `demographics` fields" entry for the full writeup. | ChartHop | ENGINEERING | closed — see `organ-capability-map.md` | organ-capability-map.md |
| paper | ~~`revisionDiff` has no version-snapshot substrate to diff against~~ **CLOSED (2026-07-16, `b8e03af1`)** — new `paper-version-save`/`paper-version-list`/`paper-version-diff` macros in `server/domains/paper.js`, modeled on `paper-annotate`'s per-paper nested-array pattern. `revisionDiff`'s line/word/char diff computation was extracted into a shared `computeTextDiff()` helper so both the pre-existing caller-supplied-text path and the new stored-snapshot path run identical logic — `revisionDiff`'s own behavior is unchanged (pinned by a byte-for-byte regression test). `paper-version-diff` runs the real diff against two actual stored snapshots (picked by sequential version number, honestly rejecting a nonexistent one). New `PaperVersionHistory.tsx` panel mounted below the notes editor in `PaperLibrary.tsx` — "Save version snapshot" persists the current live notes content, and a compare UI picks two real versions from a select. Tests: 20 new backend (81/81 combined, 0 regressions, incl. an explicit `revisionDiff` regression check) + 13 new frontend. | Zotero / Semantic Scholar / arXiv Sanity | ENGINEERING | closed | paper-capability-map.md |
| parenting | No paid-tier personalized AI sleep-coaching / real-time expert chat | Huckleberry / Cozi | ENGINEERING | "No dedicated sleep-coach backend exists" | parenting-capability-map.md |
| parenting | ~~No general shared family calendar (pediatric appointments only)~~ **CLOSED (2026-07-16, `10fd710e`)** — new `event-add`/`list`/`update`/`delete`/`ical` macros, structurally cloned from `appointment-*` but with childId genuinely OPTIONAL (a family event may not belong to any one child) and a broader non-medical category set. Real RFC-5545 export with correct all-day exclusive-DTEND math. New `PgFamilyCalendarPanel.tsx` mounted as a new top-level "Family Calendar" destination tab (not nested inside the child-gated Baby Care section). | Huckleberry / Cozi | ENGINEERING | closed | parenting-capability-map.md |
| personas | Persona chat is a deterministic `composeReply`, not an LLM completion | Character.AI | ENGINEERING / DATA-SOURCING | "a backend behavioral addition beyond a frontend-rebuild scope" | personas-capability-map.md |
| pharmacy | ~~No doctor-appointment manager/calendar~~ **CLOSED (2026-07-16, `6290da47`)** — new `appointment-add`/`list`/`update`/`delete` + `appointments-due` macros, modeled on `refill-request`/`list`/`update`/`refills-due`. Real status state machine (`scheduled` → `completed`/`cancelled`/`missed`, each terminal) locking status/dateTime once terminal while keeping notes editable afterward. `relatedMedId` is optional and validated via the existing `findMed` lookup. New `RxAppointmentsPanel.tsx` with a real medication picker (not free text), mounted as a new "Appointments" tab. | Medisafe / GoodRx | ENGINEERING | closed | pharmacy-capability-map.md |
| pharmacy | No AI-powered adherence prediction | Medisafe / GoodRx | ENGINEERING | "clearly out of scope" | pharmacy-capability-map.md |
| pharmacy | No live geocoded physical pharmacy locator | Medisafe / GoodRx | DATA-SOURCING | "a new integration (e.g. NPI registry or a commercial pharmacy-locator API)" | pharmacy-capability-map.md |
| philosophy | ~~No cross-cutting library search across the curation substrate~~ **CLOSED (2026-07-16)** — the backend macro `philosophy-search` (searches channel titles/descriptions + block content across all 5 block kinds, `server/domains/philosophy.js`) was already real and tested; the gap was UI-only. New "Search" tab (`SearchTab` in `concord-frontend/components/philosophy/PhilosophyCuration.tsx`) adds an Enter-to-search input rendering distinct Channels/Blocks result sections (blocks show kind + excerpt + owning-channel title), an honest empty-query prompt and no-results state, and a deep-link from a block result into the Image Grid tab focused on that block's channel. No backend change was needed. Tests: 5 new frontend (`tests/components/PhilosophyCuration.test.tsx`), all green; eslint clean. | Are.na / Kialo / SEP-IEP | ENGINEERING | closed | philosophy-capability-map.md |
| photography | ~~`photography.vision` macro has zero caller — button calls a generic chat endpoint instead~~ **CLOSED (`3ab3152c`)** | Lightroom (implied) | ENGINEERING | "outside a single-lens rebuild's scope" | photography-capability-map.md |
| photos | ~~`photos.get` (single-photo detail/lightbox view) has no UI caller~~ **CLOSED (`4d5e2fae`)** | none stated | ENGINEERING | "a legitimate but low-priority follow-on" | photos-capability-map.md |
| physics | ~~Richer multi-body/Keplerian/grid-based engines are shadowed (dead) by simpler `server.js` re-registrations~~ **CLOSED (2026-07-16, `fcb7f7d9`):** deleting the `server.js` duplicates so the richer engine wins under the original name was rejected as unsafe — `PhysicsAdvancedLab.tsx` and its pinned `physics-behavior.test.js` depend byte-for-byte on the flat, shadowed shape. Instead the same already-written `orbitalMechanics` handler (Keplerian-element propagation + a real Hohmann-transfer Δv/time calculation) is also registered under a new additive name, `orbitalMechanicsAdvanced`, that nothing shadows — zero logic changed, purely an additional registration. Hohmann-transfer math independently re-derived from the standard Curtis/Vallado formulas and verified against both the live macro (near machine precision) and published reference figures for the classic LEO→GEO scenario. A dedicated regression test proves the shadowed `physics.orbitalMechanics` name — the one `PhysicsAdvancedLab.tsx` actually calls — is still byte-identical, with no Hohmann/orbit-point keys leaking through under the old name. New `PhysicsKeplerianLab` panel. 5 new backend tests (43/43 combined with the untouched `physics-behavior.test.js`) + 3 new frontend tests, 0 regressions. | none stated | ENGINEERING | closed | physics-capability-map.md |
| plumbing | No permit/setback code-reference library | ServiceTitan / Jobber | CURATION | "needs licensed/curated IPC/UPC text... deferred pending a real source" | plumbing-capability-map.md |
| plumbing | No browsable materials/spec catalog | ServiceTitan / Jobber | CURATION | "needs sourced reference data this domain doesn't have" | plumbing-capability-map.md |
| plumbing | ~~No persisted Client/CRM entity~~ **CLOSED (2026-07-12, `342616e3`)** — `server/domains/plumbing.js` gained a small persisted `clients` store plus the exact `clientAdd`/`clientList` macro pair the disposition named, with an optional `clientId` param wired additively (free-text still works unchanged) into `dispatchAssign`/`invoiceFromQuote`/`planCreate`. A new `ClientAutocomplete.tsx` combobox (search-or-add-inline, full keyboard nav) replaced the raw client `<input>` in `FieldServiceConsole`'s job/invoice/plan forms. | ServiceTitan / Jobber | ENGINEERING | "flagged for a future ENGINEERING unit" | plumbing-capability-map.md |
| plumbing | ~~No municipal inspection scheduling/pass-fail records~~ **CLOSED (2026-07-16, `09f0a81f`)** — new `inspectionAdd`/`inspectionList`/`inspectionUpdate` macros distinct from the existing internal-QA path: each inspection links to a real dispatch assignment (`assignmentId`, validated against `dispatchAssign`/`dispatchBoard` records) and re-derives `jobTitle`/`jobFound` live; a required `jurisdiction` (AHJ) field is what makes this "municipal" rather than internal. 6 real categories (rough-in, top-out DWV, water service/backflow, gas line pressure test, water heater install, final plumbing). Pass/fail/pending lifecycle mirrors the masonry/landscaping precedent (fail requires deficiency notes, optional re-inspection date). New "Inspections" tab in `FieldServiceConsole.tsx` with a job picker sourced from the real dispatch board (not free text). This closes plumbing's entire ENGINEERING-class deferred list — only the two CURATION items (permit-code library, materials catalog) and the geocoding item (needs a real API) remain. Tests: 12 new backend + 8 new frontend, all independently re-run green. | ServiceTitan / Jobber | ENGINEERING | closed | plumbing-capability-map.md |
| plumbing | ~~No formal technician certification records~~ **CLOSED (2026-07-12, `4bb4a8b4`)** — `server/domains/plumbing.js` gained a distinct `certifications` array per tech (`techCertAdd`/`techCertList`/`techCertRemove`) alongside the existing freeform `skills` tag list (untouched — different purpose), with `techList`/`techCertList` deriving `isExpired` at read time. New `TechCertifications.tsx` inline disclosure (real category picker + issuing body/license #/issue/expiry fields, expired badges) wired into `FieldServiceConsole`'s technician roster. See `plumbing-capability-map.md` for the full writeup + test list. | ServiceTitan / Jobber | ENGINEERING | "real, buildable gap — now closed" | plumbing-capability-map.md |
| plumbing | No job-site geocoding/map (address string only, no lat/lng) | ServiceTitan / Jobber | ENGINEERING (needs geocoding API) | "faking coordinates would be a direct honesty violation. Deferred" | plumbing-capability-map.md |
| podcast | ~~`trimSilence` preference stored but no real silence-detection engine backs it~~ **CLOSED (2026-07-16):** `lib/podcast/silence-detect.ts` — real RMS-threshold Web Audio analysis (windowed RMS over decoded PCM, `findSilenceRanges` merges below-threshold windows ≥1.5s). `PodcastStreamPlayer.tsx` runs it when the toggle is on (aborts cleanly on toggle-off/unmount), auto-skips `currentTime` across a detected silent range on `onTimeUpdate`, and persists the preference through the existing real `playback-prefs-set` macro (unchanged backend). No backend changes were needed or made — preference storage was already real; only the missing detection engine was built. RMS math hand-verified against a synthetic 100Hz/amp-0.5 sine wave (`amp/√2`) and independently reproduced. 19 new lib tests + 3 new component tests, all passing; no regressions. | Apple Podcasts / Spotify / Buzzsprout | ENGINEERING | closed | podcast-capability-map.md |
| poetry | ~~Collection-tab search limited to titles (`poem-list` omits body for cost reasons)~~ **CLOSED (`cd0dff1e`)** | none stated | DATA-SOURCING (small backend) | "a real, small backend change I did not make" | poetry-capability-map.md |
| privacy | **INVESTIGATED (2026-07-12), reframed not just confirmed:** the DSAR "deletion" status tracker is indeed disconnected from real data, as originally described — but Concord already has a SEPARATE, real, live account-deletion pipeline (`server/lib/account-lifecycle.js`, mounted at `POST /api/account/delete`) the DSAR flow was simply never wired to. Tracing that real pipeline surfaced its own confirmed defects: an empirically-verified bug where its "delete social content" step silently fails on 100% of its 4 target tables (2 don't exist, 2 have column-name mismatches swallowed by try/catch), zero functional test coverage, several personal-data categories never covered at all (chat history, connector OAuth tokens, the encrypted personal-DTU locker, sign-in OAuth links, world/avatar/player state), and declared `ON DELETE CASCADE` FKs that are inert platform-wide (no `PRAGMA foreign_keys=ON` anywhere). Full findings, empirical repro, and a per-category HARD-DELETE/ANONYMIZE/REDACT-AND-RETAIN/OUT-OF-SCOPE policy ledger in `docs/PRIVACY_DSAR_DELETION_INVESTIGATION.md` — this is the completed follow-up investigation the prior row's disposition asked for. No code changed; needs an owner decision between 4 named options. | Google Data & Privacy / OneTrust | ENGINEERING | "touches the deletion/tombstone invariant... deserves its own careful pass" | privacy-capability-map.md, PRIVACY_DSAR_DELETION_INVESTIGATION.md |
| privacy | Cookie-banner/retention-policy config stored but not enforced by a runtime consent-gate middleware | Google Data & Privacy / OneTrust | ENGINEERING | "a retention-sweep heartbeat + a consent-gate middleware would close it" | privacy-capability-map.md |
| privacy | Access log breadth depends on more subsystems calling `recordAccess` | Google Data & Privacy / OneTrust | ENGINEERING | "Not faked — an empty log honestly shows 'No data accesses recorded yet.'" | privacy-capability-map.md |
| productivity | ~~No reminder delivery channel (on-demand check only)~~ **CLOSED (2026-07-16, `9e0780dc`)** — new `productivity-reminder-sweep` heartbeat (~30s cadence, kill-switch `CONCORD_PRODUCTIVITY_REMINDER_SWEEP=0`) pushes a `productivity:reminder-fired` socket event to a user's room the instant a time-based reminder becomes due, modeled on the existing `emitToUser`/`emitToUserRoom` pattern in `domains/collab.js`/`domains/message.js`. Honest scope: this is real-time delivery to a currently-connected tab, not OS-level push — this codebase has no service-worker Web Push pipeline, so claiming desktop/push notification delivery would be fabricating a capability that doesn't exist; the frontend labels this explicitly ("while this tab is open") and keeps the manual due-check as an honest fallback. A disconnected user still gets their reminder correctly marked fired server-side. 17 new backend tests, 7 new frontend tests. | Todoist / TickTick, Linear | ENGINEERING | closed | productivity-capability-map.md |
| productivity | ~~Recurring-task natural-language editing not available in task detail view~~ **CLOSED (`7cf853e5`)** | Todoist / TickTick, Linear | ENGINEERING | "Small frontend build; deferred to Wave 4" | productivity-capability-map.md |
| projects | ~~`view-run` (execute a saved custom board view) has no UI~~ **CLOSED (`9e3ca63b`)** | Linear / Asana / Jira | ENGINEERING | "low-priority Linear-parity edge, not a defect" | projects-capability-map.md |
| projects | ~~`integration-link` (attach external tracker item) unsurfaced~~ **CLOSED (`9e3ca63b`)** | Linear / Asana / Jira | ENGINEERING | "deferred" | projects-capability-map.md |
| projects | ~~`label-update` unsurfaced~~ **CLOSED (`9e3ca63b`)** | Linear / Asana / Jira | ENGINEERING | "deferred" | projects-capability-map.md |
| quests | **CONFIRMED (2026-07-12, not just "may"):** authored onboarding/main-arc/faction/sub-world quest content (127 quests) is unreachable by any live player-facing surface — empirically verified by booting a migrated DB, running the real seeder, and inspecting both stores: 127 quests land in the in-memory engine (`server/emergent/quest-engine.js`), 0 rows land in the SQL table (`world_quests`) the dialogue-offer/QuestTracker/quests-lens path actually reads. Root cause is THREE non-communicating quest stores, not two, plus two more break points found in the same trace (procedural quests' objectives/rewards are also always empty; the onboarding "First Cycle" tutorial reads a 4th table nothing ever writes to). Full citations + 4 candidate fix directions (none safe/small enough to just do) in `docs/QUESTS_ENGINE_INVESTIGATION.md` — this is the completed follow-up investigation the prior row asked for. | none stated | ENGINEERING | "needs an owner architectural decision between 4 named options — not a safe scoped fix" | quests-capability-map.md, QUESTS_ENGINE_INVESTIGATION.md |
| realestate | ~~No `cma_generate` macro — Comparative Market Analysis engine entirely missing~~ **CLOSED (`78514425`)** — real CMA engine drawing comps from the user's own tracked listings, honest empty result when there aren't enough. | Zillow / Redfin | ENGINEERING | "out of scope for a wiring-only pass" | realestate-capability-map.md |
| realestate | ~~`tick_rentals` has no heartbeat — rent never collected automatically~~ **CLOSED (2026-07-16, `e1a7c52a`)** — new self-registering `real-estate-rent-collection` heartbeat (`server/domains/real-estate.js`, ~1h cadence) calls the real `tickRentals` engine on a schedule. Also fixed a genuine pre-existing production bug found while verifying: `server/domains/real-estate.js` (the hyphenated in-world property-market domain, distinct from the personal-CRM `realestate.js`) was never imported by `server/domains/index.js`, so the entire `real_estate` domain — including `WorldPropertiesPanel.tsx`'s marketplace/owned-buildings/rentals UI — was dead code since it shipped; fixed with a two-line additive registration, verified empirically against a real server boot. New honest "auto-collects ~&lt;time&gt;" per-rental indicator; manual button relabeled as an explicit fallback. 8 new backend tests + 13 pre-existing, 21/21 passing; 6 new frontend tests. | Zillow / Redfin | ENGINEERING | closed | realestate-capability-map.md |
| reasoning | ~~`counterArgumentGen`'s critique quality is a fixed rule set, not a real argumentation-theory engine~~ **CLOSED (2026-07-16):** `counterArgumentGen` now accepts an optional `mapId`, adding two more real attack angles when a persisted argument map resolves — "weak-link" (the map's own `weakestClaim`/`contested` result, reusing `argumentMapCore`'s existing strength computation via a new `persistedMapToClaims` flattener, only surfaced when genuinely contested) and "scheme-critical-question" (the real Walton-style `criticalQuestions` from the `SCHEMES` library entry the map was instantiated from, when the map used a named scheme). Still a rule-based critique engine by design — the broadening is more real signals, not invented inference. Frontend replaces a raw `JSON.stringify` dump with a real badged card list per attack type. Deliberately did NOT wire the page's local `selectedMapId` into the new param (it's client-only and never resolves server-side — would have been a silently-no-op fake affordance); a real wire-up needs `ArgumentMapStudio`'s selected-map id lifted up first. 8 new backend tests + 4 new frontend tests, all passing; no regressions. | Kialo | ENGINEERING | closed | reasoning-capability-map.md |
| reflection | ~~24 of 45 macros remain UNSURFACED (media/place/reminder/encryption/timeline/map/voice/year-in-review/export/sync layer)~~ **CLOSED 2026-07-12** — re-verified: all 43 registered macros (not 45, corrected count) are wired; 15 were already covered by pre-existing `JournalStudio.tsx`, the remaining 7 (`entry-detail`/`entry-update`/`entry-summarize`/`reflect-deepen`/`journal-stats`/`reflection-goal-set`/`reflection-goal-status`) closed by commit `5c33c6ab` (batch 6, `docs/FRONTEND_REBUILD_PROGRAM.md` ledger). Original triage kept for history: | Day One | ENGINEERING | "backend already supports all of it" — now stale, see reflection-capability-map.md's 2026-07-12 update | reflection-capability-map.md |
| repos | ~~`codeComplexity`'s heuristic is a regex count, not a real AST parse~~ **CLOSED (2026-07-16, `ba5bee97`):** new `server/lib/code-ast-complexity.js` walks the real TypeScript-compiler AST (same package + lazy-load pattern `ts-language-service.js` already uses — zero new dependencies), detecting real function boundaries (FunctionDeclaration/Expression/Arrow/Method/Get/Set/Constructor) and McCabe cyclomatic complexity via the same decision-point set ESLint's `complexity` rule uses. Proven to be a real parse, not a regex, by a test asserting a comment/string containing literal `"if ("` text scores zero branches. `ConcordRepoWorkspace.tsx`'s old client-side regex counter (`estimateFileComplexity`) is deleted; the macro now takes real `sourceFiles`, falling back honestly (`ast_engine_unavailable`, never silent fabrication) if the TS package fails to load. 9 new hand-traced contract tests + repos-domain-parity coverage, 0 regressions (66/66 combined). | GitHub | ENGINEERING | closed | repos-capability-map.md |
| repos | ~~`security-scan`'s vulnerability table is a hardcoded 5-entry CVE list, not a live feed~~ **CLOSED (`a9f2710d`)** — real OSV.dev feed wired. | GitHub | DATA-SOURCING | "could wire a real free vulnerability feed (OSV.dev)" | repos-capability-map.md |
| retail | ~~No CRM/sales-pipeline macro~~ **CLOSED (2026-07-16, `cb45c52b`)** — new `deals-list`/`deals-upsert`/`deals-stage-move`/`deals-delete` macro family: a real SMB CRM funnel (lead→contacted→qualified→proposal→negotiation→won/lost) with every stage change appended to an auditable `stageHistory` (never a mutable label), won/lost terminal with an explicit `reopen` path back into an open stage only, and server-computed rollups (total/weighted pipeline value, per-stage counts). `pipelineValue`'s existing pasted-book calculator falls back to reading this persisted book only on TRUE omission of both `deals`/`opportunities` keys — narrowed from the initial draft, which would have fired on any malformed value and silently changed the pre-existing "garbage in → empty pipeline" contract (caught by re-running the pre-existing test that covers exactly that case). New `PipelinePanel.tsx`: a real kanban by stage, designed create form, stage-move control per card (no drag-and-drop primitive existed in the codebase to reuse), won/lost archive with reopen. Tests: 28 new backend (`retail-deals-pipeline.test.js`) + 148/148 across all 5 retail backend test files combined + 7 new frontend. | Shopify / Square / Lightspeed | ENGINEERING | closed | retail-capability-map.md |
| retail | ~~No support-ticket queue macro~~ **CLOSED (2026-07-16, `e9c4f7fd`)** — new `tickets-list`/`tickets-upsert`/`tickets-status-move`/`tickets-reply-add`/`tickets-delete` macro family: a real support-desk lifecycle (open→in-progress→waiting-on-customer→resolved→closed), `closed` a locked terminal requiring an explicit `reopen:true` back into an open status, `resolve` stamping `resolvedWithinSla` against a per-priority SLA deadline, and a real reply thread. SLA target-minutes is a single shared constant (`TICKET_PRIORITY_SLA_MINUTES`) reused by both the new queue and the pre-existing `slaStatus` incidents branch, so the two never disagree. `slaStatus`'s legacy tickets branch falls back to this persisted queue only on true key-omission (matching the `pipelineValue` fix from the CRM unit, applied proactively here and verified against the pre-existing malformed-payload test, which still passes byte-identically). New `TicketQueuePanel.tsx`: a real list/detail split view with priority + SLA-countdown badges, status filters, a reply composer, resolve/reopen actions. Tests: 24 new backend (standalone run) + 206/206 across all 6 retail backend test files combined + 9 new frontend. | Shopify / Square / Lightspeed | ENGINEERING | closed | retail-capability-map.md |
| retail | ~~No in-store marketing-display/endcap record macro~~ **CLOSED (2026-07-16, `3f0dfc3d`)** — new `displays-list`/`displays-upsert`/`displays-status-move`/`displays-log-impressions`/`displays-record-conversion`/`displays-delete` macro family: a real physical-merchandising record (endcap/window/checkout-counter/floor-display/shelf-talker/promotional-table), distinct from the existing digital `campaigns-*` family, with `productSkus` validated against the real product catalog and a `planned→active→removed` lifecycle (`removed` locked, requires explicit reopen). Impressions are honestly a manually-logged, accumulating count (no automated foot-traffic sensor exists in Concord — the macro is named `log`, not `track`). Conversions mirror `campaigns-record-conversion`'s existing honesty discipline exactly: `displays-record-conversion` requires a real `orderId` from the caller's own order book, rejects unknown/fake ids, guards double-attribution. `revenuePerBudgetDollar` is honestly `null` (never `Infinity`/`NaN`) when budget is 0. New `DisplaysPanel.tsx`: a real merchandising board with a log-impressions quick action and a record-conversion flow gated on a real order. Tests: 42 new backend + 248/248 across all 7 retail backend test files combined + 12 new frontend, 33/33 combined retail frontend regression. | Shopify / Square / Lightspeed | ENGINEERING | closed | retail-capability-map.md |
| retail | ~~Product schema lacks variants, price-change history, supplier fields, ABC-analysis forecasting~~ **CLOSED (2026-07-16, `f5541272`) — closes ALL FOUR of retail's originally-deferred items.** `product-upsert` extended with `supplier`/`leadTimeDays`/`dailySalesRate`, non-destructively preserved from the existing record when omitted (mirrors the pre-existing `createdAt` pattern) — the old minimal `{sku,name,price,stock}` call shape stays byte-identical, proven against the pre-existing product-catalog tests. `priceHistory` is server-computed only, auto-appending on a real price change. `turnoverRate` = `(dailySalesRate × 365) / stock`, honestly `null` at stock=0. `abcClass` is a real Pareto/ABC bucketing by revenue proxy, ranked catalog-wide, classified by cumulative share of every product ranked ABOVE it (avoids the boundary-overshoot bug that would misclassify a single dominant SKU). New `product-price-history`/`product-variant-upsert`/`product-variant-list`/`product-variant-delete` — variants are genuinely separate sub-SKU records (own SKU/stock/price-delta, parent-SKU validated, cascade-deleted with the parent). New `ProductCatalogPanel.tsx` replaces the old thin `CatalogTab` (not a second competing surface) with ABC badges, turnover rate, a price-history timeline, and a variants sub-list. Tests: 36 new backend + all 8 retail backend test files re-run together (284/284, 0 regressions) + 11 new frontend + 44/44 combined retail frontend regression. | Shopify / Square / Lightspeed | ENGINEERING | closed | retail-capability-map.md |
| robotics | No 3D URDF/mesh robot viewer (Gazebo/rviz-style) | ROS + rviz/Gazebo, Foxglove Studio | ENGINEERING | "materially larger scope" | robotics-capability-map.md |
| robotics | No simulated physics/collision environment | ROS + rviz/Gazebo, Foxglove Studio | ENGINEERING | "a physics sim is out of scope for a UI-parity pass" | robotics-capability-map.md |
| sandbox | ~~No visible frame-data overlay in TelemetryOverlay, though backend endpoint exists~~ **CLOSED (`1a429211`)** | Street Fighter 6 / Tekken Practice | ENGINEERING | "Left out of this pass to keep the fix surgical" | sandbox-capability-map.md |
| security | ~~Legacy "Domain Actions" buttons silently no-op with no feedback on a fresh account~~ **CLOSED (`b971070d`)** | Splunk / Sentinel / OpenCVE / PagerDuty | ENGINEERING | "cosmetic... Left as a backlog item" | security-capability-map.md |
| self | ~~Achievements tab has no unlock-timestamp tracking (bare `Set`, no `earned_at`)~~ **CLOSED (`d78e9519`)** — achievements now a `Map` carrying a real `earnedAt` timestamp. | Apple Health / Gyroscope | ENGINEERING | "Not changed this pass" | self-capability-map.md |
| sentinel | ~~`intel.research.*` governance-controlled research-access workflow completely unsurfaced~~ **CLOSED (2026-07-16)** — closed two real backend gaps alongside the frontend: (1) `reviewResearchApplication` (`server/lib/foundation-intelligence.js`) was imported into `server.js` but never registered as a macro, so every submitted application was permanently stuck "pending" with no way to ever reach approved/denied — new `intel.research.review` macro (role-gated `owner`/`admin`/`founder`, same inline-check shape as `goals.approve`) closes the dead end; (2) `research.apply`/`.status`/`.data`/`.synthesis`/`.archive` previously trusted a client-supplied `researcherId` — all five now derive identity from `ctx.actor` server-side, closing a real cross-user impersonation gap. New "Research" tab (`SentinelResearchAccess.tsx`) in the Sentinel lens: apply form, per-browser application tracking re-verified live against `research.status` on every render (honest — no fabricated "my applications" list, since the backend has no list-by-researcher export), gated Tier-2 data/synthesis/archive pulls once approved, and a role-gated (not a fabricated admin panel — hidden entirely for non-reviewers) review-by-ID affordance for the rare owner/admin/founder session, since no pending-queue listing exists server-side either. Tests: 10 new backend (`server/tests/depth/sentinel-research-access-behavior.test.js`) + 13 new frontend (`concord-frontend/tests/components/SentinelResearchAccess.test.tsx`), all green; 188 pre-existing sentinel + foundation-intelligence tests unaffected; eslint clean. | CrowdStrike Falcon | ENGINEERING | "deserves its own designed workflow" (closed) | sentinel-capability-map.md |
| services | Card processing honestly gated to pay-on-site — no real Stripe Elements/Terminal client-confirmation flow | Square Appointments / Booksy / Vagaro | ENGINEERING | "deferred out of this pass's scope" | services-capability-map.md |
| sim | ~~`monteCarlo`'s RNG uses `Math.random()`, not seeded — results not reproducible~~ **CLOSED (2026-07-12, `df11c27d`)** — `monteCarlo` now accepts an optional `seed` (`params.seed` or `artifact.data.seed`), reusing the existing `makeRng` mulberry32 PRNG already used by `agentBased`/`discreteEvent`; same seed → byte-identical result (deep-equal, pinned by tests), omitted seed → the original `Math.random()` path unchanged. Optional "Fixed seed (reproducible)" toggle added to `SimToolkit.tsx`'s Monte Carlo tool. | AnyLogic | ENGINEERING | "not urgent enough to bundle in" | sim-capability-map.md |
| space | No live satellite catalog beyond ISS (needs authenticated account) | N2YO / Flightradar24-for-space / Go4Liftoff | DATA-SOURCING | "not a free/keyless API. Noted honestly rather than faked" | space-capability-map.md |
| space | ~~No ground-station pass scheduling for a player's own satellites~~ **CLOSED (2026-07-16, `3dcfa1a0`)** — `orbitalPeriodMinutes(altitudeKm)` extracted from `orbitCalc`'s original inline formula into a shared helper reused by `orbitCalc`/`satellite-list`/`satellite-passes` (proven identical by a cross-macro test). New `satellite-track`/`satellite-list`/`satellite-untrack`/`satellite-passes` macros — a user's own satellite has no live ephemeris source (unlike the real `iss-passes`, which samples wheretheiss.at), so `satellite-passes` is an explicitly-labeled `precision:"estimated"` analytical estimate: an inclination-band gate returns an honest zero passes for a station outside the reachable-latitude band, never fabricated visibility. `OwnedSatellites.tsx` is visually distinct from the live ISS panels (amber dashed framing, persistent "ESTIMATED — not live" badge). 27 new backend tests, 13 new frontend tests. | N2YO / Flightradar24-for-space / Go4Liftoff | ENGINEERING | closed | space-capability-map.md |
| spectate | ~~Socket events carry no `worldId` — spectators of one world also see events from every other world~~ **CLOSED (Wave 4, platform-wide half)** — `combat:hit` (server.js's `combat:attack` socket handler) now stamps a real worldId via `cityPresence.getUserPosition(userId).worldId` (the pre-existing `cityPresence.getPlayerWorld` accessor this pass would have reused turned out to be dead code — undefined on every call, always silently falling back to `"concordia-hub"` — so the fix routes through the real, populated field instead); `world:event:scheduled` already carried worldId (`lib/world-event-scheduler.js#tick` merges it in — that part of the original claim was stale, verified via `git blame`), so only its `event-shapes.js` registration was missing; the three `faction:*` events now stamp a best-effort worldId via a new `resolveFactionWorldId` helper (`lib/embodied/faction-strategy.js`, de-duplicated from a same-shaped helper that already existed in `emergent/faction-strategy-cycle.js` for a different purpose) resolved from the faction's living NPCs; `dtu:promoted` stamps worldId only when the promoted DTU actually carries one (DTUs are cross-world by design — CLAUDE.md's DTU substrate notes — so this is often honestly absent, never invented). `event-shapes.js` schemas updated (worldId optional on all 4, since at least one real emit path for each can't always supply it). The spectate `[worldId]` ticker's `push()` filter now genuinely exercises worldId for all 6 previously-unfiltered events, and gained a `faction:truce-sought` subscription it never had. `EmergentEventFeed.tsx` needed no change — it already read `p.worldId` opportunistically for display and is correctly a platform-wide dashboard that doesn't filter. Pinned by `server/tests/contract/heartbeat-emits.test.js`, `server/tests/world-event-scheduler.test.js`, `server/tests/wave4-event-worldid.test.js`, `concord-frontend/tests/lenses/spectate-worldid-ticker.test.tsx`. | Twitch / Kalshi-Polymarket hybrid | ENGINEERING | closed — see `spectate-capability-map.md`'s "Honest residual, not fixed" paragraph for the full before/after | spectate-capability-map.md |
| staking | Neither staking system ever calls `walletDebit`/`walletCredit` — no real CC moves; positions don't survive restart | Coinbase Earn / Lido | ENGINEERING | "deferred to human escalation" per CLAUDE.md's money-invariant rule | staking-capability-map.md |
| studio | ~~Real-time collaborators can join/see presence, but no mutating macro is collaborator-aware — guest edits silently fail~~ **CLOSED (2026-07-12, `508399c7`)** — every mutating `studio.*` macro (and the paired project/track/clip/lane list+get reads) now resolves through a collaborator-aware helper family (`resolveStudioProject`/`resolveOwnerBucketItem`/`resolveTrackOwner`/etc.): a caller holding an active `collab-join` seat reads/writes the HOST's real project bucket, not their own empty one, while a caller who never joined (nor owns the project) still gets a clean "not found" in both directions. See `studio-capability-map.md`'s "Real-time collaboration honestly doesn't grant real write access" section for the full fix + regression-test detail. | Ableton Live / Logic Pro | ENGINEERING | closed — see `studio-capability-map.md` | studio-capability-map.md |
| studio | ~~`collab-edit` logging wired for only 6 of ~19 mutating panels~~ **CLOSED (`03ff59a5`)** — extended to all 18 real mutating panels (re-derived count; `CollabPanel` is the log's reader, not a mutator). | Ableton Live / Logic Pro | ENGINEERING | "extending it is mechanical, one call per mutation handler" | studio-capability-map.md |
| studio | ~~Project picker doesn't auto-refresh cross-panel (separate `PipingProvider` trees)~~ **CLOSED (2026-07-16, `a69f4dc7`):** the single `PipingProvider` was lifted up to wrap both `DawWorkbenchSection` and `StudioActionPanel` in one tree (previously only `StudioActionPanel` was inside a provider); `DawWorkbenchSection` now subscribes via `usePipeValue('studio.project')` and re-fetches its project list whenever `StudioActionPanel` publishes a newly created project — zero manual interaction. The manual ⟳ refresh button stays as the honest fallback. No backend change was needed. A dedicated test mounts both real components under one provider and proves the picker updates without ever clicking refresh (and was sanity-checked load-bearing by reverting the fix and confirming the test fails). | Ableton Live / Logic Pro | ENGINEERING | closed | studio-capability-map.md |
| sub-worlds | ~~Spawned sub-world's mirrored `worlds` row is a bare-minimum shell (no presets/NPCs/biome/theme)~~ **CLOSED (`7269d318`)** — kind-appropriate physics/rule-modulator presets + a deterministic 2-NPC starter roster now seeded on spawn. | Roblox / Rec Room | ENGINEERING | "the next real increment" | sub-worlds-capability-map.md |
| sub-worlds | ~~Legacy `sub_world.spawn_from_forge` macro has the identical never-mirrors defect~~ **CLOSED (`7269d318`)** — turned out worse than documented (never mirrored to `worlds` at all, so "Enter" 404'd); now fixed alongside the primary spawn path. | Roblox / Rec Room | ENGINEERING | "flagged for a future backend-hygiene pass" | sub-worlds-capability-map.md |
| supplychain | No role-based collaboration (planner/buyer/analyst views) — all macros scope only to the calling user | SAP Integrated Business Planning (IBP) | ENGINEERING | "out of scope for a frontend rebuild" | supplychain-capability-map.md |
| sync | `sync.heartbeat` has zero frontend callers — no real client identity yet | Dropbox / iCloud Drive / Syncthing | ENGINEERING | "ships only if/when a real client exists to be honest about" | sync-capability-map.md |
| sync | ~~Real portable-pack export isn't wired into this lens's `sync_now`~~ **CLOSED (2026-07-16, `4b8a50b5`)** — new `export_pack` macro wires the existing, tested `exportUserCorpus` engine (`dtu-portability.js`, untouched) into this lens for the first time, matching both prior real callers' usage exactly. Device-scope filtering was deliberately NOT faked: `sync_now`'s in-memory scope filter has no reliable equivalent against the persisted `dtus` SQL table's free-form data, so `export_pack` honestly exports the real full corpus labeled `scoped:false` rather than shipping a filter that looks scoped but isn't reliably correct. New "Download portable pack" button per device in `SyncDashboard.tsx`, reusing the existing shared `downloadFile` helper. | Dropbox / iCloud Drive / Syncthing | ENGINEERING | closed | sync-capability-map.md |
| thread | ~~No cross-artifact "duplicate this thread" / whole-thread clone feature~~ **CLOSED (2026-07-16, `09ee34bd`)** — new `thread.thread-clone` macro deep-copies content/platform/posts and mints fresh media ids, modeled on the `thread-draft`/`draft-delete` per-user CRUD pattern. Clone always resets to draft status regardless of the source's status, matching Typefully's real edit-and-repost use case. `ThreadComposer.tsx` gets a Duplicate button beside the existing Delete. 21 new backend tests, 4 new frontend tests. | Typefully | ENGINEERING | closed | thread-capability-map.md |
| timeline | ~~`friends`-tier privacy not enforced on id-addressed macros (only `private` tier fixed this pass)~~ **CLOSED (`1934f90b`)** — security fix. | Facebook | ENGINEERING | "a genuine ENGINEERING-class gap per CLAUDE.md's 'closing the hard 20%' triage" | timeline-capability-map.md |
| tournaments | ~~`server/domains/tournaments.js` state is in-memory only — doesn't survive restart~~ **CLOSED (2026-07-12, `f688402f`)** — migration 360 adds a real `bracket_tournaments` table; the domain now reads/writes through a db-or-memory store facade (same pattern as `domains/saved.js`/mig 356), durable whenever `ctx.db` is present, falling back to the legacy in-memory Map only for minimal/test builds with no DB handle. All 12 macros' response shapes unchanged; payout/prize-pool math untouched. See `tournaments-capability-map.md` for the table schema and `server/tests/tournaments-persistence.test.js` for the persistence proof (raw-SQL + second-DB-handle checks). | Challonge / Battlefy | ENGINEERING/CURATION | closed — see `tournaments-capability-map.md`'s "Investigated and honestly deferred" section for the before/after | tournaments-capability-map.md |
| training-room | `combo_followups` metadata never authored — combo-followups UI strip always empty | Street Fighter 6 / Tekken training room | CURATION | "a CURATION-class gap in the skill-authoring system" | training-room-capability-map.md |
| travel | ~~No inbox auto-sync (Gmail/Outlook) for booking confirmations~~ **CLOSED (2026-07-16, `be583c83`):** Gmail only (Concord has no Outlook connector). Extracted `booking-import`'s inline email-parsing into a standalone `parseBookingEmail()` so manual paste and the new sync path can never drift apart (regression-tested byte-identical). New `travel.inbox-sync` macro rides the same SSRF-guarded connector path `gmail.js`'s own `list`/`get` actions use (read-only imports — `gmail.js` itself untouched) and mirrors its honest reason-code contract, never fabricating a "synced" result when Gmail isn't connected. Imports only at confidence ≥2/4 signals; dedupes on resync via a `sourceMessageId` stamped onto synced bookings. New `GmailSyncPanel` in `TripWorkspace`'s Bookings tab. 11 new backend tests + 7 new frontend tests, 0 regressions (139/139 combined travel suite). | TripIt / Hopper | ENGINEERING | closed | travel-capability-map.md |
| travel | ~~No binary attachment support on travel documents~~ **CLOSED (2026-07-16, `2efe8b43`)** — new `travel-doc-attachment-upload`/`-list`/`-download`/`-delete` macros, structurally cloned from `server/domains/projects.js`'s "[M] Binary file attachments" pair — base64 payload validation, a 5 MB per-file cap, heavy blob never returned from list views. New per-user `travelDocAttachments` state bucket following `travel.js`'s own Map-per-user idiom; ownership isolation falls out of that pattern for free. `travel-doc-list` embeds attachment metadata per document. New file-input "Attach file" + download/delete UI in `TravelDocsPanel.tsx`. 17 new backend tests, 7 new frontend tests. | TripIt / Hopper | ENGINEERING | closed | travel-capability-map.md |
| travel | ~~No loyalty-program/frequent-flyer points tracking~~ **CLOSED (2026-07-16, `bc8fe753`):** new `loyalty-account-add/-list/-update/-remove` macros (per-user CRUD cloned from the `travel-doc-*` idiom, optional `tripId` link) plus a separate append-only `loyalty-points-log-add/-list` ledger keyed by account id. The account never stores a points balance — it's derived by summing the ledger on every read (`loyalty-account-list`, `loyalty-points-log-list`, and on every write), the same derive-don't-store principle `travel-doc`'s `expiryStatus` already follows. Removing an account cascades its ledger. New "Loyalty" tab (`LoyaltyProgramsPanel`) in `TravelTripsSection`. 12 new backend tests + 9 new frontend tests, 0 regressions (128/128 combined travel suite). | TripIt / Hopper | ENGINEERING | closed | travel-capability-map.md |
| travel | No airport terminal maps | TripIt / Hopper | DATA-SOURCING | "No data source; low value" | travel-capability-map.md |
| travel | No live bookable flight/hotel search + pricing (inspiration-only today) | Google Flights / Kayak / Expedia | DATA-SOURCING | "a real, disclosed, paid-third-party-API-required gap" | travel-capability-map.md |
| understanding | ~~No spaced-repetition surface, no nested/outline note structure~~ — **CLOSED (2026-07-12):** re-audit found Concord already has a full separate Anki/FSRS-parity SRS lens (`server/domains/srs.js`) — the real gap was narrower (RemNote's "review notes in place" model). Built: note-level SM-2 review scheduling (`understanding.review`/`due`, hand-verified interval math) + real nested outline structure reusing the `link` substrate via a reserved relation (`understanding.move`/`reorder`/`outline`, single-parent + no-cycle enforced) + `OutlineView`/`ReviewQueue` UI. 19 new tests, 36/36 passing. | Obsidian / RemNote | CURATION/ENGINEERING | "a CURATION/ENGINEERING triage for a future session, not a rebuild-loop defect" | understanding-capability-map.md |
| urban-planning | ~~No honest project/permit-status tracking (fake tab was removed outright)~~ **CLOSED (2026-07-16, `98e1b84a`)** — new `project-add`/`project-list`/`project-update`/`project-status-update`/`project-remove` macros in `server/domains/urbanplanning.js`, mirroring `parcel-add`/`parcel-list`/`parcel-remove`'s CRUD shape and `comment-add`/`comment-list`/`comment-resolve`'s status-transition pattern. A six-stage lifecycle (`proposed`/`approved`/`under_construction`/`built`/`denied`/`cancelled`) is hard-rejected on an invalid value at `project-status-update` — unlike `comment-resolve`'s soft default, since a civic project's permit status is a load-bearing public record. Every transition appends to a `statusHistory` audit trail. New `ProjectTracker.tsx` panel (optional real parcel link, forward-only status button group, expandable history timeline). Tests: 28 new backend (89/89 combined, 0 regressions) + 15 new frontend. | Esri CityEngine / ArcGIS Urban, CommunityViz | ENGINEERING | closed | urban-planning-capability-map.md |
| urban-planning | No live parcel/GIS basemap tiles | Esri CityEngine / ArcGIS Urban, CommunityViz | DATA-SOURCING | "needs a tile-provider integration decision (cost/ToS)" | urban-planning-capability-map.md |
| urban-planning | ~~No shadow/sun-path 3D massing study~~ **CLOSED (2026-07-16, `4bb344d5`)** — new `shadowStudy` macro computes real solar position via the NOAA Solar Calculator algorithm crossed with a real massing envelope's height via shadow trig, sampled hourly across a UTC day. Explicitly labeled a 2D shadow-path study, not a rendered 3D massing study. Hand-verified against a closed-form solar-noon reduction and cross-checked against a published external reference (NYC June-solstice altitude). New "Sun / Shadow Study" tab in `ZoningSiteAnalysis.tsx` with a real hourly table + labeled 2D schematic. 25 new backend tests, 4 new frontend tests. | Esri CityEngine / ArcGIS Urban, CommunityViz | ENGINEERING | closed | urban-planning-capability-map.md |
| voice | ~~`recording-auto-label-speakers` genuinely unreachable (no per-segment audio `.vector`)~~ **CLOSED (2026-07-16):** `recording-create` and `live-append` now accept an optional per-segment/per-append `vector` (validated when present, honestly rejected when malformed, fully backward-compatible when absent); `live-finalize` folds per-word vectors into a running-mean per-speaker-group vector (cloning `voiceprint-enroll`'s existing accumulation shape). The Web Audio feature extraction that used to live only in `VoiceprintEnroll.tsx` is now shared (`lib/voice/audio-features.ts`); `VoiceLiveTranscribe.tsx` opens a parallel mic tap using it to compute a real vector per finalized segment (honest no-op — no vector sent — when mic/Web-Audio access is unavailable). `VoiceRecordingStudio.tsx` adds an "Auto-label speakers" button rendering the macro's real per-segment match result (enrolled speaker, distance, confidence) rather than just a count. 9 new backend tests (hand-verified running-mean math + a genuine close-match/far-no-match pair against real enrolled voice-prints) + 17 new frontend tests (extraction math, live-transcribe vector wiring, auto-label button/rendering), all passing; 94 pre-existing voice-domain tests unaffected. | none stated | ENGINEERING | closed | voice-capability-map.md |
| vote | ~~`fairnessCheck`'s Gallagher index needs a seat-share input the UI doesn't collect~~ **CLOSED (`1e45e3ba`)** — real seat-allocation editor in `BallotAnalysisLab`. | Decidim / Snapshot / Polis | ENGINEERING | "would need its own bespoke editor" | vote-capability-map.md |
| welding | ~~Token-based client portal (`portal-view`/`approve`/`pay`) unsurfaced — no public route~~ **CLOSED (`09d63112`)** | Jobber / ServiceTitan | ENGINEERING | "a genuinely deferred build" | welding-capability-map.md |
| whiteboard | ~~`vision` macro (image-analysis of uploaded board image) unsurfaced~~ **CLOSED (2026-07-12, `f6df1526`)** — image embeds (`embed.kind === 'image'`) are the existing "image element pin"; `WhiteboardCollabPanel.tsx`'s Embeds tab now renders an "Analyze" action (eye icon) on each image embed that calls `whiteboard.vision` with the embed's URL and shows the returned description inline, or an honest failure message on an unreachable/blocked vision brain — never a fabricated success. 4 new frontend tests (button scoped to `kind === 'image'` only, correct macro call shape, loading state, honest-failure rendering) + 4 new backend tests pinning the macro's own contract (missing-input rejection, honest failure through `callVision` when the brain is unreachable, SSRF-guard rejection for loopback + cloud-metadata URLs) — all passing. | Miro / FigJam (implied) | ENGINEERING | "not a defining whiteboard-category feature" | whiteboard-capability-map.md |
| whiteboard | ~~`comments-delete` unsurfaced~~ **CLOSED (`17897cc2`)** | Miro / FigJam (implied) | ENGINEERING | "reasonable for a future pass" | whiteboard-capability-map.md |
| whiteboard | ~~`frame-update` unsurfaced~~ **CLOSED (`17897cc2`)** | Miro / FigJam (implied) | ENGINEERING | "not a defining feature" | whiteboard-capability-map.md |
| whiteboard | ~~`embed-update` unsurfaced~~ **CLOSED (`17897cc2`)** — title-rename only; the macro has no `url` field (doc's "swap URL" framing was inaccurate). | Miro / FigJam (implied) | ENGINEERING | "same low priority" | whiteboard-capability-map.md |
| whiteboard | ~~`workspace-summary` unsurfaced~~ **CLOSED (`17897cc2`)** | Miro / FigJam (implied) | ENGINEERING | "Low priority, purely additive" | whiteboard-capability-map.md |
| whiteboard | ~~`shared-vote-cast`: vote-count badges render, but no click-to-vote handler exists~~ **CLOSED (`17897cc2`)** | Miro / FigJam (implied) | ENGINEERING | "the most concrete near-term gap of the eight" | whiteboard-capability-map.md |
| world-creator | ~~`npc-place`'s `backstory`/`level` fields accepted server-side, UI only captures name via `window.prompt`~~ **CLOSED (`d1a2df87`)** | Tiled / RPG-Maker-style placer | ENGINEERING | "a small, scoped frontend addition, no backend change" | world-creator-capability-map.md |
| world-creator | ~~No `zone-move`/`spawn-move`/`npc-move` macros (delete+recreate required)~~ **CLOSED (2026-07-16, `0e7c1adf`)** — new `zone-move`/`spawn-move`/`npc-move` macros in `server/domains/world-creator.js`, each mirroring `prop-move`'s exact clamp-and-patch shape. The frontend drag gesture was ALSO fixed — it was hardwired to props only in four places (drag state carried no entity kind, `onMove` always reported `'prop'`, the type signature was narrowed to `'prop'`, and zone/npc/spawn SVG elements had no `onMouseDown` drag-start handler at all, only click-to-select) — widened to all four kinds in `SceneCanvas.tsx`, with new `updateZone`/`updateSpawn`/`updateNpc` optimistic-update callbacks in `DraftEditor.tsx` mirroring `updateProp`'s pattern. This closes world-creator's entire ENGINEERING-class deferred list. Tests: 18 new backend (0 regressions) + 8 new/extended frontend. | Tiled / RPG-Maker-style placer | ENGINEERING | closed | world-creator-capability-map.md |
| world-creator | ~~Zone/spawn-point names never user-set at creation~~ **CLOSED (`d1a2df87`)** | Tiled / RPG-Maker-style placer | ENGINEERING | "not fabricated, just always-default" | world-creator-capability-map.md |

## Lenses with no deferred items (fully closed, per own doc)

achievements, affect, agriculture, all, analytics, announcements, answers, app-maker,
attention, auction, audit, bio, black-market, bounties, bridge, billing,
carpentry, chem, classroom, code-quality, cognition, cognitive-replay, commonsense, construction,
cooking, crafting, creative, creative-writing, cri, crisis-ops, crypto, custom, database,
death-insurance, debate, debug,
defense, detective, disputes, diy, docs, electrical, environment, ethics, expedition-journal,
expert-mode, federation, film-studios, finance, fishing, forestry, forge, fork, forum, fractal,
gallery, game-design, garage, genesis, geology, ghost-tracker, global, goddess, government,
home-improvement, household, housing, hvac, import, inference,
invariant, lattice, ledger, legacy, lfg, linguistics, maker, manufacturing, market, markets,
meditation, mental-health, metalearning, move-builder, neuro, nonprofit, observe, ocean, ops, pets, platform,
psyops, quantum, queue, repair-telemetry, research, resonance, saved, schema, science, sessions,
settings, social, society, sponsorship, sports, srs, system, tick, telecommunications, temporal,
tools, trades, transfer, translation, veterinary, wallet, wellness, worldmodel

`world-capability-map.md` is a summary-level backfill doc (not an exhaustive audit) — it makes no
gap claims either way and is excluded from both lists.

## Notes on compound triage

A handful of items span two classes and are marked accordingly in the Triage column:
- **`ingest`** live connector execution and **`integrations`** real OAuth connect flow — both
  ENGINEERING (the connectorFetch/OAuth plumbing) *and* DATA-SOURCING (each concrete connector
  needs its own credential/egress path).
- **`personas`** LLM-backed persona chat — ENGINEERING (wiring the chat path) with a DATA-SOURCING
  flavor (needs a live brain call, not a deterministic composer).
- **`tournaments`** durable persistence and **`understanding`** spaced-repetition/outline —
  ENGINEERING/CURATION: the backend rebuild is engineering, but doing it well benefits from
  curated reference patterns (Challonge-style bracket persistence; RemNote-style note graphs).

## Next step

This ledger is the input to Wave 4 execution, not the execution itself. Per CLAUDE.md's sixth
hard invariant, each row still needs an actual disposition: DATA-SOURCING items need a real
source identified (or an honest "no free source exists" writeup); ENGINEERING items need to
actually be built; CURATION items need real reference material authored/ingested. Given the
volume (259 items), the natural next move is to prioritize a subset — likely candidates are the
CURATION items (small, bounded, well-scoped: healthcare protocol library, landscaping/plumbing
code-reference libraries, training-room combo metadata) and the highest-value DATA-SOURCING items
with a known free source already named in the doc (e.g. `repos` security-scan → OSV.dev,
`grounding` → GDELT) — rather than attempting all 259 in one pass.
