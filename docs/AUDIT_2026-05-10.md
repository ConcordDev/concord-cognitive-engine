# Audit pass — 2026-05-10

> **📌 HISTORICAL SNAPSHOT (dated 2026-05-10).** This is a point-in-time audit at
> HEAD `41794550`; its numbers were correct *then* and are intentionally NOT
> updated (rewriting a dated audit would falsify the record). The codebase has
> grown substantially since — for current counts run `npm run check-doc-claims`
> / `npm run count-loc`, or see CLAUDE.md's re-verified table (2026-06-02).

Independent verification of every numeric claim and load-bearing invariant in
`CLAUDE.md` and `docs/AUDIT_INVENTORY.md` against the working tree at HEAD
`41794550` (post PR #318 + PR #325 + #326). Every line below was checked by
direct `grep`/`ls`/`wc` — no claim was taken on faith.

**Status:** all drift findings in §"Drift findings" below have been merged
back into `CLAUDE.md` and `docs/AUDIT_INVENTORY.md` in the same commit pair
that introduces this report. `npm test` (server) was also run on a clean
checkout: 10,940 main + 1304 behavior = **12,244 passing**, with 1 timeout
flake on `tests/detectors-suite.test.js` (env-sensitive, not assertion).

## What lines up

| Claim | Reality | Source |
|---|---|---|
| Backend domain files: 210 | 210 | `ls server/domains/*.js \| wc -l` |
| Migrations: 158, latest `158_kingdoms.js` | 158 / `158_kingdoms.js` | `ls server/migrations/[0-9]*.js \| sort \| tail -1` |
| Route files: 130 | 130 | `ls server/routes/*.js \| wc -l` |
| Emergent modules: 166 | 166 | `ls server/emergent/*.js \| wc -l` |
| Lib modules: 292 top-level / 485 recursive | 292 / 485 | `ls server/lib/*.js`, `find server/lib -name '*.js'` |
| HTTP routes in `server.js`: 1086 | 1086 | regex grep |
| CREATE TABLE statements: 374 | 374 | grep across migrations |
| Unique macro domains: 129 | 129 | grep `register(` |
| Unique heartbeats: 50 | 50 | grep `registerHeartbeat(`, named list matches CLAUDE.md exactly |
| `MAX_ROYALTY_RATE` = 0.30 | 0.30 | `server/economy/royalty-cascade.js:263` |
| `ROYALTY_FLOOR` 0.0005, `MAX_CASCADE_DEPTH` 50, `INITIAL_ROYALTY_RATE` 0.21, `ROYALTY_HALVING` 2 | all match | `server/lib/creative-marketplace-constants.js:427-430` |
| `MARKETPLACE_FEE` 0.04 / `CREATOR_SHARE` 0.70 / `ROYALTY_SHARE` 0.20 / `TREASURY_SHARE` 0.10 / `TOKEN_PURCHASE_FEE` 0.0146 | all match | `server/server.js:63297-63301` |
| DTU royalty-aware path: `creatorPool = price * 0.95`, `platformFee = price * 0.05` | match | `server/server.js:33767-33768` |
| `WITHDRAWAL_HOLD_HOURS = 48` | match | `server/economy/withdrawals.js:23` |
| Citation consent gate (`citation_consent_not_granted`, license bypass, parent toggle) | match | `server/economy/royalty-cascade.js:63-84` |
| Combat anti-cheat: `_validateCombatReach` + `_validateDamageCap` declared and called | match | `server/routes/worlds.js:44, 66, 1874, 1908` |
| World-event rewards mint via `mintCoins` with `event_reward:${eventId}:${userId}` refId | match | `server/lib/world-events.js:185` |
| `player_inventory.world_id` (migration 101) | match | `server/migrations/101_player_inventory_world_scope.js:20` |
| Refusal-field algebra (`computeFieldComposition`, `glyphAdd`, `isCompoundRefusal`) | match | `server/lib/refusal-field.js:175-231` |
| NPC secret leakage gate (narrative bridge canary scan + structural omission) | match | `server/lib/narrative-bridge.js:105-115, 184, 220` |
| Lattice orchestrator init wired post circuit-breakers | match | `server/server.js:54754` (`initLatticeOrchestrator(STATE)` call) |
| All Layer 7–13 lib + emergent files exist on disk | match | direct `ls` |
| Sprint D files exist (`kingdoms.js`, `kingdom-decrees.js`, `kingdom-takeover.js`, `kingdom-rebellion.js`, `kingdom-decree-cycle.js`, `npc-scheme-cycle.js`) | match | direct `ls` |
| Sprint D tables (`character_opinions`, `realms`, `realm_*`) exist in 153 / 158 | match | grep migrations |
| Five-brain init (`initFiveBrains`) + `BRAIN_VISION_URL` declared | match | `server/server.js:14690, 13528` |
| Heartbeat overrun observability (`concord_heartbeat_ticks_total`, `concord_heartbeat_skipped_total`, `_governorTickRunning` guard) | match | `server/server.js:6263, 6284, 30803` |
| `TICK_FREQUENCIES.CONSOLIDATION = 30` | match | `server/server.js:2140, 31290` |

## Drift findings (CLAUDE.md / inventory disagree with reality)

### 1. Lens directory count is **206**, not 205
- `ls -d concord-frontend/app/lenses/*/ \| wc -l` → **206**
- Both `CLAUDE.md` and `docs/AUDIT_INVENTORY.md:13` claim 205.
- `docs/AUDIT_INVENTORY.md:532` separately says "verified inventory (203 dirs)" — internally inconsistent with the same file's own line 13.
- PR #318 added `concord-frontend/app/lenses/understanding/page.tsx` 12 minutes before the inventory was refreshed; the count was not re-run.

### 2. Layer 7–11 migration numbers in `CLAUDE.md` are wrong by 4–5
The current text claims:

| Layer | CLAUDE.md says | Actual on disk |
|---|---|---|
| Layer 7 — `embodied_signal_log` | migration 108 | **112** (`112_embodied_signals.js`; 108 is `lattice_train_consent`) |
| Layer 8 — `pain_signals` | migration 109 | **114** (109 is `brain_interactions`) |
| Layer 9 — `dreams` | migration 110 | **115** (110 is `affect_state`) |
| Layer 10 — `forward_predictions` | migration 111 | **116** (111 is `qualia_state`) |
| Layer 11 — `faction_strategy_state` + `faction_relations` + `faction_strategy_log` | migration 112 | **117** (`117_faction_strategy.js`) |
| Layer 13 — `npc_conversations` | migration 118 | 118 ✓ |

Layers 7–11 were renumbered into 112–117 (with `113_embodied_signal_log_unification.js` as a glue migration); CLAUDE.md was not updated to follow.

### 3. May 2026 Concordia game-feel pass migration numbers in `CLAUDE.md` are off by 1–3

| Item | CLAUDE.md says | Actual |
|---|---|---|
| Deferred D4 — `player_signs` | migration 146 | **149** (146 is `repair_feedback`) |
| Deferred D3 — `quest_triggers` + `quest_trigger_visits` | migration 147 | **150** (147 is `mount_polish`) |
| Deferred D2 — `player_corpses` | migration 148 | **151** (148 is `signal_propagation_indexes`) |

### 4. Macro pair count is **686**, not 671
- Inventory says 671 with footnote "true post-Sprint-D pair count is ≥ 690".
- Direct grep against the same `register('domain','name')` pattern yields **686**.
- CLAUDE.md says 671 without the caveat.

### 5. Combined HTTP route count off by 1
- `server.js`: 1086 ✓
- `routes/*.js`: **1313** (CLAUDE.md says 1314, inventory says 1313)
- Combined: **2399** (CLAUDE.md says 2400)

### 6. `server.js` line-number citations are stale by ~600–2,300 lines
The monolith grew (now 67,784 lines, was ~62k when these were written):

| Citation | CLAUDE.md says | Actual |
|---|---|---|
| `concord_heartbeat_ticks_total` declaration | server.js:5592 | **server.js:6263** |
| `concord_heartbeat_skipped_total` declaration | server.js:5636 | **server.js:6284** |
| `concord_heartbeat_skipped_total` increment | server.js:28800 | **server.js:30803** (the `_governorTickRunning` guard is at 30803–30811; the early-return path lives in this block) |
| DTU auto-compression in `governorTick` | server.js:28970-29061 | **server.js:31290** |
| quest-engine `await import()` fix | server.js:42591 | not verified at that line — needs re-grep |

`narrative-bridge.js:147` (CLAUDE.md) — actual secret-omission code is at `:184` (omission) + `:220` (canary scan); `:147` is harmless `currentGoal` field assignment.

### 7. Wrong path for fauna spawner / creature behaviors
- CLAUDE.md (Concordia game-feel theme 2): `fauna-spawner.js` and `creature-behaviors.js` are described as if living next to each other.
- Actual: both are under `server/lib/ecosystem/`, not `lib/world/`. The heartbeat module is at `server/emergent/creature-flock-cycle.js`.

### 8. `domains/forge.js` does not exist
- CLAUDE.md (Phase B / Forge wire-up): "Domain macros: `forge.list`, `forge.sections`, `forge.validate`, `forge.generate` (parity with HTTP route via runMacro)".
- The macros DO exist (`server/server.js:28864-28882`), but they are registered inline in `server.js`, not in a `server/domains/forge.js` file. A reader following the documented pattern "Domain logic lives in `server/domains/<domain>.js`" will not find them where the doc implies.

### 9. Forge file sizes drifted
- CLAUDE.md: `lib/forge-template-generator.js` 1575 LOC. Actual: **1579**.
- CLAUDE.md: `emergent/forge-template-engine.js` 1418 LOC. Actual: **1472**.

### 10. Stale "17 heartbeats registered" prose
The "Heartbeat-registry pattern" bullet under "Fully working end-to-end" still
says "**17 heartbeats** registered (per cartographer)" with a 17-name list. The
true count is now 50; the list of 17 is a strict subset of the current 50. The
ground-truth number a few sections higher (50) is correct — this is just a
prose paragraph that wasn't followed forward.

## Tests

- 380 test files in `server/tests/`, plus 1 elsewhere = 381 total.
- The test glob in `server/package.json:test` is `tests/**/*.test.js` evaluated
  with `cwd=server/`, so CLAUDE.md's `tests/foo.test.js` references are
  consistent **when read from the server directory**. Outside the server dir
  they would not resolve.
- I could not run the full suite (this working tree has no
  `server/node_modules`, so `better-sqlite3` is missing and any test importing
  the DB layer fails with `ERR_MODULE_NOT_FOUND` before its first assertion).
  Of three sampled targeted tests, two passed cleanly:
  - `combat-anti-cheat.test.js` — 5/5
  - `lattice-orchestrator.test.js` — 5/5
  - `royalty-cascade.test.js` — fails on import (env, not code)

## PR #318

Merged 2026-05-10T02:14:35Z. 12,277 additions / 1,033 deletions / 163 files /
66 commits. Phase A (deep-rebuild of `crafting`, `creator`, `federation`),
Phase B (74 productization-roadmap promotions), Phase C (10 system lenses
wired through `useArtifacts` / `useCreateArtifact`), Phase D (closed 4
cartographer-flagged gaps including the new `/lenses/understanding` page
that wired all 16 previously-headless `understanding` macros). Touch is
frontend + `server/domains/crafting.js`; no migrations, no heartbeats, no
new routes. The lens count miscount in §1 of this audit traces directly to
this PR landing 12 minutes before the inventory refresh.

## Recommended doc fixes (next pass)

1. Re-run `ls -d concord-frontend/app/lenses/*/ | wc -l` and update the lens
   count to 206 in BOTH `CLAUDE.md` and `docs/AUDIT_INVENTORY.md` (and fix
   the "203 dirs" subhead in the inventory).
2. Replace migration 108–112 callouts in the Layer 7–11 sections with the
   actual numbers 112, 114, 115, 116, 117 (Layer 13's 118 stays).
3. Replace migration 146/147/148 callouts in the May 2026 Concordia
   game-feel pass with 149/150/151.
4. Re-run the macro-pair count script (the footnote says it is broken) and
   bump to 686.
5. Either re-grep `server.js` line numbers and update them, or replace
   line-number citations with grep'pable anchors (`registerHeartbeat("foo"`
   etc.) that survive monolith growth.
6. Fix the `lib/world/fauna-spawner.js` path → `lib/ecosystem/fauna-spawner.js`.
7. Either create `server/domains/forge.js` (move the inline registrations
   there) or change the wording so the doc no longer implies a file that
   doesn't exist.
8. Drop the stale "17 heartbeats" paragraph or replace it with a pointer to
   the `docs/AUDIT_INVENTORY.md` 50-name list.
